# CSP-J/S 模拟（二）

**日期**：2026-08-17

**说明**：J组不需要做附加题，S组需要做J组的所有题目及附加题。

---

## 一、单项选择题

- 共 15 题，每题 2 分，共计 30 分
- 每题有且仅有一个正确选项

1. 以下哪个序列对应数字 0 至 7 的 4 位二进制格雷码（ ）。
   A. 0000, 0001, 0011, 0010, 0110, 0111, 0101, 1000
   B. 0000, 0001, 0011, 0010, 0110, 0111, 0101, 0100
   C. 0000, 0001, 0011, 0010, 0100, 0101, 0111, 0110
   D. 0000, 0001, 0011, 0010, 0110, 0111, 0100, 0101

2. 以下判断一个正整数 n 是否是偶数的代码中，错误的是（ ）。
   A. if (n % 2 == 0)
   B. if (!(n % 2))
   C. if (n & 1)
   D. if (n % 2 != 1)

3. 在 C++ 中，5 ^ -5 和 5 & -5 的值分别为（ ）。
   A. -10, 5
   B. -2, -5
   C. -2, 1
   D. -10, 1

4. 一棵哈夫曼树，拥有 13 个叶结点。则该哈夫曼树的结点总数为（ ）。
   A. 25
   B. 26
   C. 27
   D. 无法确定

5. 下列关于树的描述中正确的是（ ）。
   A. 有 n 个结点的树，边数只能是 n - 1 条。
   B. 在哈夫曼树中，叶子结点的比非叶子结点多 1 或 2 个。
   C. 完全二叉树是二叉排序树。
   D. 在二叉树的后序遍历序列中，若结点 u 在结点 v 之前，则 u 一定是 v 的祖先

6. 下面代码构成的是（ ）类型的数据结构。
   A. 双向链表
   B. 栈
   C. 循环链表
   D. 队列

7. 以下代码调用 F(n) 的时间复杂度为（ ）。
   A. Θ(1)
   B. Θ(n)
   C. Θ(n²)
   D. Θ(2ⁿ)

8. 在简单图中，有 n 个顶点的强连通图最少有（ ）条边，最多有（ ）条边。
   A. n - 1, n
   B. n - 1, n(n - 1)
   C. n, n(n - 1)
   D. n, n(n - 1)/2

9. 以下关于强连通图的说法中，正确的是（ ）。
   A. 图中一定有环
   B. 每个顶点的度数都大于 0
   C. 对于大于 1 个点的强连通图，任意两个顶点之间都有路径相连
   D. 每个顶点至少都连有一条边

10. 用线性探测法把 k 个关键字相同但内容不同的数据存入哈希表中，至少要进行（ ）次探测。
   A. k + 1
   B. k - 1
   C. (k + 1) · k/2
   D. (k - 1) · k/2

11. 求 a,b,c,d,e,f 六个字母的全排列中不允许出现 ace 和 df 子串的排列数（ ）。
   A. 177
   B. 377
   C. 477
   D. 582

12. 一堆卡片共 2025 张，设定一个数量，不断从牌堆中取该数量的牌，记 A 表示该数量为素数的方案数，记 B 为该数量为奇数的方案数。则 A + B =（ ）。
   A. 16
   B. 17
   C. 18
   D. 19

13. 正整数 n ≥ 3 称为理想数，若存在正整数 k(1 ≤ k ≤ n - 1) 使得 C(n, k - 1)、C(n, k)、C(n, k + 1) 构成等差数列（其中 C(n, k) 为组合数，且 C(n, k) = n! / (k!(n - k)!)），则不超过 2026 的理想数的个数为（ ）。
   A. 43
   B. 44
   C. 45
   D. 46

14. 已知一棵二叉树有 2026 个结点，则其中至多有（ ）个结点有 2 个子结点。
   A. 1010
   B. 1011
   C. 1012
   D. 1013

15. 从乘法算式 1 × 2 × 3 × 4 × ··· × 24 × 25 中，最少要删掉（ ）个数才能使剩下的数的乘积是完全平方数。
   A. 3
   B. 4
   C. 5
   D. 6

---

## 二、阅读程序

- 判断题 1 分，选择题 3 分，共计 40 分
- 判断题正确填 T ，错误填 F；

### 第1题

```cpp
bool check(int n, int a[])
{
    bool found = false;
    for (int i = 0; i + 1 < n; ++i) {
        if (a[i] > a[i+1]) {
            found = true;
            auto temp = a[i];
            a[i] = a[i+1];
            a[i+1] = temp;
        }
    }
    return found;
}

void solve(int n, int a[])
{
    while (check(n, a))
        ;
}
```

判断题

16. solve 函数的 while 语句只写一份分号会产生语法错误（ ）。

17. 当输入参数 n ∈ [0,1024] 时，solve 可能会进入死循环（ ）。

18. 当 n=5，a = [1, 2, 3, 4, 5] 时，while 循环只运行一次（ ）。

19. 当 n=5，a = [5, 4, 3, 2, 1] 时，while 循环运行两次（ ）。

20. 当 solve 结束后，a 数组以降序排列（ ）。

选择题

21. 当输入为 n=5，a = [3, 1, 4, 1, 5] 时，程序结束时，a =（ ）。
   A. [1, 3, 1, 4, 5]
   B. [5, 4, 3, 1, 1]
   C. [1, 1, 3, 4, 5]
   D. [3, 1, 4, 1, 5]

22. 程序交换 a[i] 与 a[i+1] 的次数（ ）。
   A. 小于数组 a 的逆序对数量
   B. 等于数组 a 的逆序对数量
   C. 大于数组 a 的逆序对数量
   D. 与数组 a 的逆序对数量无关

23. 程序的最坏时间复杂度是（ ）。
   A. Θ(n log n)
   B. Θ(log n)
   C. Θ(n)
   D. Θ(n²)

### 第2题

```cpp
bool move(int b[], int n)
{
    for (int i = 0; i < n; ++i) {
        if (b[i] == 1) {
            b[i] = 0;
        }
        else {
            b[i] = 1;
            return true;
        }
    }
    return false;
}

void print(int n)
{
    int b[n];
    for (int i = 0; i < n; ++i) b[i] = 0;
    do {
        for (int i = 0; i < n; ++i) std::cout << b[i];
        std::cout << "\n";
    }
    while (move(b, n));
}

int count(int n)
{
    int b[100];
    for (int i = 0; i < n; ++i) b[i] = 0;
    int c = 0;
    do {
        c++;
    }
    while (move(b, n));
    return c;
}
```

判断题

24. 当输入参数 n ∈ (0,20) 时，count 可能会陷入无尽的循环（ ）。

25. print 输出的 0 与 1 必然一样多（ ）。

26. print(6) 输出的第 5 行第 4 列的字符为 0（ ）。

27. print(16) 输出的第 523 行第 14 列的字符为 1（ ）。

选择题

28. count(2) 的返回值为（ ）。
   A. 1
   B. 2
   C. 3
   D. 4

29. print(n) 与 count(n) 的时间复杂度为（ ）。
   A. Θ(2ⁿ), Θ(2ⁿ)
   B. Θ(n · 2ⁿ), Θ(2ⁿ)
   C. Θ(n · 2ⁿ), Θ(n · 2ⁿ)
   D. Θ(4ⁿ), Θ(2ⁿ)

30. 以下说法错误的是（ ）。
   A. print(n) 前一半输出全是偶数，后一半输出全是奇数
   B. 若将 print(n) 输出的每行内容看成一个数字，则他们是依次递增的
   C. print(n) 输出的每一行内容都是不同的
   D. print(n) 输出的每一列内容都是不同的

### 第3题

```cpp
const int mod = 1'000'000'007;
bool filled[maxn][maxn];
int mem[maxn][maxn];

int solve(int i, int j, int a[], int b[])
{
    if (i == 0 || j == 0) {
        return 1;
    }
    if (filled[i][j]) {
        return mem[i][j];
    }
    filled[i][j] = true;
    int sum = (solve(i-1, j, a, b) + solve(i, j-1, a, b)) % mod;
    if (a[i] == b[j]) {
        return mem[i][j] = sum;
    }
    else {
        return mem[i][j] = (sum - solve(i-1, j-1, a, b)) % mod;
    }
}
```

判断题

31. 若 a[1] = 1, a[2] = 3, b[1] = 3, b[2] = 1, solve(2, 2, a, b) 返回 3（ ）。

32. solve(0, 0, a, b) 返回 1（ ）。

33. solve(n, n, a, a) 返回 2ⁿ。（ ）。

34. 函数 solve 的返回值可能小于 0（ ）。

选择题

35. 该程序的主要功能是（ ）。
   A. 计算两个序列的最长公共子序列的长度
   B. 计算两个序列的公共子序列的数量
   C. 计算两个序列的公共子串的数量
   D. 计算两个序列的相同元素个数

36. solve(n, m, a, b) 的最坏时间复杂度为（ ）。
   A. Θ(n + m)
   B. Θ(n · m)
   C. Θ(2ᵐ⁺ⁿ)
   D. Θ(n log m)

37. 当 a[i] != b[j] 时，返回值需要减去 solve(i-1, j-1, ...) 的原因是（ ）。
   A. 排除重复情况
   B. 排除错误情况
   C. 优化程序的空间效率
   D. 优化程序的时间效率

---

## 三、完善程序

- 单选题，每小题 3 分，共计 30 分

### 第1题 套餐限价

有一家快餐店，出售 N 种主食，其价格以数组 A[0..N) 表示，出售 M 种饮料，其价格以数组 B[0..M) 表示。
现推出一种促销活动：顾客可以任选主食及饮料各一份形成套餐，若套餐价格超过一个给定的最高价格 L，则这份套餐只收取 L 元。
请计算，若顾客购买所有食物与饮料的搭配（共有 N × M 种），需要花多少钱。

```cpp
long long S[MAXN];
long long solve(int N, int M, int L, int A[], int B[])
{
    std::sort(A, A + __(1)__);
    std::sort(B, B + M);
    S[0] = 0;
    for (int i = 0; i < M; ++i) {
        S[i+1] = S[i] + B[i];
    }
    long long j = __(2)__;
    long long sum = 0;
    for (int i = 0; i < N; ++i)
    {
        while (__(3)__ && A[i] + B[j-1] > L)
        {
            j--;
        }
        sum += (A[i] * __(4)__);
        sum += S[__(5)__];
        sum += (M - j) * L;
    }
    return sum;
}
```

38. (1)处应填（ ）。
   A. N-1
   B. N
   C. N+1
   D. M

39. (2)处应填（ ）。
   A. M-1
   B. M
   C. M+1
   D. N

40. (3)处应填（ ）。
   A. j > 0
   B. j >= 0
   C. j > 1
   D. j < M

41. (4)处应填（ ）。
   A. j - 1
   B. j
   C. j + 1
   D. i

42. (5)处应填（ ）。
   A. i - 1
   B. i
   C. j - 1
   D. j

### 第2题 简单路径

给定 N 个点，M 条边，构成一个图。请统计从 1 号出发，有多少条简单路径。所谓简单路径，就是路径上的所有点及所有边不会重复出现两次。如果路径超过 1024 条，则输出 -1。

```cpp
#include<iostream>
#include<vector>
int N, M;
std::vector<int> adj[200001]; // 邻接表
bool visited[200001];
const int limit = 1024;
int cnt = 0;
void dfs(int node)
{
    __(1)__;
    if (cnt > limit) return;
    visited[node] = __(2)__;
    for (auto v : __(3)__)
    {
        if (not visited[v])
        {
            dfs(__(4)__);
        }
    }
    visited[node] = __(5)__;
}
int main() {
    std::cin >> N >> M;
    for (int i = 0; i < M; ++i) {
        int A, B;
        std::cin >> A >> B;
        adj[A].push_back(B);
        adj[B].push_back(A);
    }
    dfs(__(6)__);
    if (cnt > limit) {
        std::cout << -1 << "\n";
    }
    else {
        std::cout << cnt << "\n";
    }
}
```

43. (1)处应填（ ）。
   A. cnt += 1
   B. cnt -= 1
   C. cnt *= 2
   D. cnt += 2

44. (2)(5)处应填（ ）。
   A. true 与 true
   B. true 与 false
   C. false 与 true
   D. false 与 false

45. (3)处应填（ ）。
   A. visited[v]
   B. adj[v]
   C. visited[node]
   D. adj[node]

46. (4)处应填（ ）。
   A. node
   B. cnt
   C. v
   D. limit

47. (6)处应填（ ）。
   A. 0
   B. 1
   C. N
   D. cnt

---

## 四、附加题

- 判断题 1 分，选择题 3 分，共计 30 分
- 判断题正确填 T ，错误填 F；

### 第1题

```cpp
#include<iostream>
void solve(int r, int b)
{
    int mem[1024] = {0};
    int q[1024] = {0};
    int step = 0;
    while (r != 0) {
        step++;
        if (mem[r] > 0) {
            break;
        }
        else {
            mem[r] = step;
        }
        q[step] = r * 2 / b;
        r = r * 2 % b;
    }
    std::cout << ".";
    if (r == 0) {
        for (int i = 1; i <= step; ++i) std::cout << q[i];
    }
    else {
        int begin = mem[r];
        for (int i = 1; i < begin; ++i)
            std::cout << q[i];
        std::cout << "(";
        for (int i = begin; i < step; ++i)
            std::cout << q[i];
        std::cout << ")";
    }
}
```

判断题

48. 若 solve(r, b) 中 r < b 且 r > 0，则输出的第一个字符一定是 0（ ）。

49. 若 solve(r, b) 中 r = 0，则程序输出 0。（ ）。

50. solve(1, 3) 的输出为 0.(0)（ ）。

选择题

51. 该程序的功能是（ ）。
   A. 将分数 r/b 转换成二进制
   B. 将分数 r/b 转换成 b 进制
   C. 将整数 r 转换成 b 进制
   D. 将整数 r 转换成二进制

52. solve(5, 8) 的输出为（ ）。
   A. 0.1
   B. 0.011
   C. 0.(011)
   D. 0.1(011)

53. solve(1, 6) 的输出为（ ）。
   A. 0.(10)
   B. 0.0(1)
   C. 0.01
   D. 0.(01)

54. solve(1, 13) 的输出为（ ）。
   A. 0.(0011100111001)
   B. 0.(001110011101)
   C. 0.0(01110011101)
   D. 0.(00111001110)

55. solve(3, 7) 的输出为（ ）。
   A. 0.(011011)
   B. 0.(01)
   C. 0.0(11011)
   D. 0.(011)

56. solve(1, 17) 的输出有（ ）位循环节。
   A. 16
   B. 8
   C. 4
   D. 2

57. solve(1, 19) 的输出有（ ）位循环节。
   A. 18
   B. 9
   C. 6
   D. 3

58. solve(1, 97) 的输出有（ ）位循环节。
   A. 48
   B. 32
   C. 16
   D. 8

59. 以下说法正确的是（ ）。
   A. solve(1, p) 的循环节长度一定是 p-1（p为质数）
   B. solve(1, p) 的循环节长度一定是 p 的因数（p为质数）
   C. solve(1, p) 的循环节长度一定小于 p（p为质数）
   D. solve(1, p) 的循环节长度无法确定（p为质数）

---

## 答案

| 题号 | 答案 | 题号 | 答案 | 题号 | 答案 | 题号 | 答案 |
|------|------|------|------|------|------|------|------|
| 1. | B | 2. | C | 3. | C | 4. | A |
| 5. | A | 6. | B | 7. | D | 8. | C |
| 9. | C | 10. | C | 11. | D | 12. | B |
| 13. | A | 14. | C | 15. | C | 16. | F |
| 17. | F | 18. | F | 19. | F | 20. | F |
| 21. | C | 22. | B | 23. | D | 24. | F |
| 25. | T | 26. | T | 27. | F | 28. | D |
| 29. | B | 30. | B | 31. | T | 32. | T |
| 33. | F | 34. | T | 35. | B | 36. | B |
| 37. | A | 38. | B | 39. | B | 40. | A |
| 41. | B | 42. | D | 43. | A | 44. | B |
| 45. | D | 46. | C | 47. | B | 48. | F |
| 49. | T | 50. | T | 51. | B | 52. | C |
| 53. | B | 54. | A | 55. | D | 56. | B |
| 57. | D | 58. | D | 59. | A |
