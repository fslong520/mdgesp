# 2026-07-23

## CodeBuddy 默认 craft agent — 接入忆时指令
- 创建 `.codebuddy/rules/忆时规则.md`，内容来自 `~/.config/opencode/skills/忆时/yishi-instructions.md` 核心要求
- 设置 `alwaysApply: true`，使每次新建对话时默认 craft agent 自动加载
- 关键行为变更：AI 自称"小妹"、称用户为"哥哥"；鲁迅式半文半白风格；三条红线（言必检/值必存/存必告）；六戒行事准则；审计三问
- 同步创建 `.codebuddy/memory/` 目录
