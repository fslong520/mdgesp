# 综合应用

## 2020年

### 第28题（2020）阅读程序：DFS数组合并（判断题）
```cpp
#include <algorithm>
#include <iostream>
using namespace std;

int n;
int d[50][2];
int ans;

void dfs(int n, int sum) {
    if (n == 1) {
        ans = max(sum, ans);
        return;
    }
    for (int i = 1; i < n; ++i) {
        int a = d[i - 1][0], b = d[i - 1][1];
        int x = d[i][0], y = d[i][1];
        d[i - 1][0] = a + x;
        d[i - 1][1] = b + y;
        for (int j = i; j < n - 1; ++j)
            d[j][0] = d[j + 1][0], d[j][1] = d[j + 1][1];
        int s = a + x + abs(b - y);
        dfs(n - 1, sum + s);
        for (int j = n - 1; j > i; --j)
            d[j][0] = d[j - 1][0], d[j][1] = d[j - 1][1];
        d[i - 1][0] = a, d[i - 1][1] = b;
        d[i][0] = x, d[i][1] = y;
    }
}

int main() {
    cin >> n;
    for (int i = 0; i < n; ++i)
        cin >> d[i][0];
    for (int i = 0; i < n; ++i)
        cin >> d[i][1];
    ans = 0;
    dfs(n, 0);
    cout << ans << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：DFS、搜索
- **第1题**：若输入 n 为 0，此程序可能会死循环或发生运行错误。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：n=0时数组越界。

### 第29题（2020）阅读程序：DFS数组合并（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：DFS、搜索
- 若输入 n 为 20，接下来的输入全为 0，则输出为 0。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：全0时所有合并的s=0，最终ans=0。

### 第30题（2020）阅读程序：DFS数组合并（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：DFS、搜索
- 输出的数一定不小于输入的 d[i][0] 和 d[i][1] 的任意一个。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：可能输出0（全0输入）。

### 第31题（2020）阅读程序：DFS数组合并（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：DFS、搜索
- 若输入的 n 为 20，接下来的输入是 20 个 9 和 20 个 0，则输出为（）
  - A. 1890
  - B. 1881
  - C. 1908
  - D. 1917
  - **答案：B**
  - **解析**：模拟DFS合并过程。

### 第32题（2020）阅读程序：DFS数组合并（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：DFS、搜索
- 若输入的 n 为 30，接下来的输入是 30 个 0 和 30 个 5，则输出为（）
  - A. 2000
  - B. 2010
  - C. 2030
  - D. 2020
  - **答案：C**
  - **解析**：模拟DFS合并过程。

### 第33题（2020）阅读程序：DFS数组合并（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：DFS、搜索
- 若输入的 n 为 15，接下来的输入是 15 到 1，以及 15 到 1，则输出为（）
  - A. 2440
  - B. 2220
  - C. 2240
  - D. 2420
  - **答案：C**
  - **解析**：模拟DFS合并过程。

## 2021年

### 第34题（2021）完善程序：Josephus问题（选择题）
```cpp
#include <iostream>
using namespace std;

const int MAXN = 1000000;
int F[MAXN];

int main() {
    int n;
    cin >> n;
    int i = 0, p = 0, c = 0;
    while (①) {
        if (F[i] == 0) {
            if (②) {
                F[i] = 1;
                ③;
            }
            ④;
        }
        ⑤;
    }
    int ans = -1;
    for (i = 0; i < n; i++)
        if (F[i] == 0)
            ans = i;
    cout << ans << endl;
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：模拟、循环
- **第34题**：①处应填（）
  - A. i < n
  - B. c < n
  - C. i < n - 1
  - D. c < n - 1
  - **答案：D**
  - **解析**：循环直到剩余n-1人，c记录已离开人数。

### 第35题（2021）完善程序：Josephus问题（选择题）
- **题型**：程序补全（选择题）
- **知识点**：模拟、循环
- **第35题**：②处应填（）
  - A. i % 2 == 0
  - B. i % 2 == 1
  - C. p
  - D. !p
  - **答案：C**
  - **解析**：报数为1的人离开，p=1时报数为1。

### 第36题（2021）完善程序：Josephus问题（选择题）
- **题型**：程序补全（选择题）
- **知识点**：模拟、循环
- **第36题**：③处应填（）
  - A. i++
  - B. i = (i + 1) % n
  - C. c++
  - D. p ^= 1
  - **答案：C**
  - **解析**：离开一人，c加1。

### 第37题（2021）完善程序：Josephus问题（选择题）
- **题型**：程序补全（选择题）
- **知识点**：模拟、循环
- **第37题**：④处应填（）
  - A. i++
  - B. i = (i + 1) % n
  - C. c++
  - D. p ^= 1
  - **答案：D**
  - **解析**：报数交替0和1，p取反。

### 第38题（2021）完善程序：Josephus问题（选择题）
- **题型**：程序补全（选择题）
- **知识点**：模拟、循环
- **第38题**：⑤处应填（）
  - A. i++
  - B. i = (i + 1) % n
  - C. c++
  - D. p ^= 1
  - **答案：B**
  - **解析**：移动到下一位，循环队列。

### 第39题（2021）完善程序：矩形计数（选择题）
```cpp
#include <iostream>
using namespace std;

struct point {
    int x, y, id;
};

bool equals(point a, point b) {
    return a.x == b.x && a.y == b.y;
}

bool cmp(point a, point b) {
    return ①;
}

void sort(point A[], int n) {
    for (int i = 0; i < n; i++)
        for (int j = 1; j < n; j++)
            if (cmp(A[j], A[j - 1])) {
                point t = A[j];
                A[j] = A[j - 1];
                A[j - 1] = t;
            }
}

int unique(point A[], int n) {
    int t = 0;
    for (int i = 0; i < n; i++)
        if (②)
            A[t++] = A[i];
    return t;
}

bool binary_search(point A[], int n, int x, int y) {
    point p;
    p.x = x;
    p.y = y;
    p.id = n;
    int a = 0, b = n - 1;
    while (a < b) {
        int mid = ③;
        if (④)
            a = mid + 1;
        else
            b = mid;
    }
    return equals(A[a], p);
}

const int MAXN = 1000;
point A[MAXN];

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> A[i].x >> A[i].y;
        A[i].id = i;
    }
    sort(A, n);
    n = unique(A, n);
    int ans = 0;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            if (⑤ && binary_search(A, n, A[i].x, A[j].y) &&
                        binary_search(A, n, A[j].x, A[i].y)) {
                ans++;
            }
    cout << ans << endl;
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：排序、二分查找
- **第39题**：①处应填（）
  - A. a.x != b.x ? a.x < b.x : a.id < b.id
  - B. a.x != b.x ? a.x < b.x : a.y < b.y
  - C. equals(a, b) ? a.id < b.id : a.x < b.x
  - D. equals(a, b) ? a.id < b.id : (a.x != b.x ? a.x < b.x : a.y < b.y)
  - **答案：B**
  - **解析**：按x坐标排序，x相同按y排序。

### 第40题（2021）完善程序：矩形计数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：排序、二分查找
- **第40题**：②处应填（）
  - A. i == 0 || cmp(A[i], A[i - 1])
  - B. t == 0 || equals(A[i], A[t - 1])
  - C. i == 0 || !cmp(A[i], A[i - 1])
  - D. t == 0 || !equals(A[i], A[t - 1])
  - **答案：D**
  - **解析**：去重，保留不与前一个相同的点。

### 第41题（2021）完善程序：矩形计数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：排序、二分查找
- **第41题**：③处应填（）
  - A. b - (b - a) / 2 + 1
  - B. (a + b + 1) >> 1
  - C. (a + b) >> 1
  - D. a + (b - a + 1) / 2
  - **答案：C**
  - **解析**：二分查找中间位置。

### 第42题（2021）完善程序：矩形计数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：排序、二分查找
- **第42题**：④处应填（）
  - A. !cmp(A[mid], p)
  - B. cmp(A[mid], p)
  - C. cmp(p, A[mid])
  - D. !cmp(p, A[mid])
  - **答案：B**
  - **解析**：A[mid]小于p时向右搜索。

### 第43题（2021）完善程序：矩形计数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：排序、二分查找
- **第43题**：⑤处应填（）
  - A. A[i].x == A[j].x
  - B. A[i].id < A[j].id
  - C. A[i].x == A[j].x && A[i].id < A[j].id
  - D. A[i].x < A[j].x && A[i].y < A[j].y
  - **答案：D**
  - **解析**：矩形对角线两点，x和y都不同。

## 2025年

### 第28题（2025）阅读程序：最长公共子序列（判断题）
```cpp
#include <algorithm>
#include <cstdio>
#include <cstring>
#define ll long long
int f[5007][5007];
int a[5007], b[5007];
int n;
int main() {
  scanf("%d", &n);
  for (int i = 1; i <= n; ++i) {
    scanf("%d", &a[i]);
  }
  for (int i = 1; i <= n; ++i) {
    scanf("%d", &b[i]);
  }
  for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= n; ++j) {
      if (a[i] == b[j]) {
        f[i][j] = f[i-1][j-1] + 1;
      } else {
        f[i][j] = std::max(f[i-1][j], f[i][j-1]);
      }
    }
  }
  printf("%d\n", f[n][n]);
  return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- **第28题**：当输入"4 1 2 3 4 1 3 2 2"时，输出为2。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：a=1234, b=1322, LCS=2（"13"或"12"）。

### 第29题（2025）阅读程序：最长公共子序列（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- 当程序运行完毕后，对于所有的 1≤i,j≤n，都一定有 f[i][j]≤f[n][n]。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：f[i][j] ≤ f[n][n] 恒成立，因为f[n][n]是最终答案。

### 第30题（2025）阅读程序：最长公共子序列（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- 将第18行的 `f[i][j]=std::max(f[i][j],std::max(f[i-1][j],f[i][j-1]));` 删去后，并不影响程序运行结果。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：删去则丢失f[i-1][j]和f[i][j-1]的继承，答案可能变小。

### 第31题（2025）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 输出的答案满足的性质有（）
  - A. 小于等于n
  - B. 大于等于0
  - C. 不一定大于等于1
  - D. 以上均是
  - **答案：D**
  - **解析**：f[n][n]≤n、≥0、不一定≥1（无公共元素时为0）。

### 第32题（2025）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 如果在16行的循环前加上以下两行：`std::sort(a+1,a+n+1); std::sort(b+1,b+n+1);`，则答案会（）
  - A. 变大或不变
  - B. 变小或不变
  - C. 一定变大
  - D. 不变
  - **答案：A**
  - **解析**：排序后LCS可能不变或变大（变成交集大小）。

### 第33题（2025）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 如果输入的a数组是1,2,...,n，而且b数组中数字均为1~n中的正整数，则上述代码等价于下面哪个问题：（）
  - A. 求b数组去重后的长度
  - B. 求b数组的最长上升子序列
  - C. 求b数组的长度
  - D. 求b数组的最大值
  - **答案：B**
  - **解析**：a={1..n}递增时，LCS等价于b的LIS。

### 第39题（2025）完善程序：精明与糊涂（选择题）
```cpp
#include <iostream>
#include <vector>
using namespace std;

int N;
bool query(int i, int j);

int main() {
    cin >> N;
    int candidate = 0;
    int count = ①;
    
    for (int i = 1; i < N; ++i) {
        if (②) {
            candidate = i;
            count = 1;
        } else {
            if (③) {
                ④;
            } else {
                count++;
            }
        }
    }
    
    cout << ⑤ << endl;
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：算法设计、逻辑推理
- **第39题**：①处应填（）
  - A. 0
  - B. 1
  - C. N
  - D. -1
  - **答案：B**
  - **解析**：count初始化为1（candidate=0计数为1）。

### 第40题（2025）完善程序：精明与糊涂（选择题）
- **题型**：程序补全（选择题）
- **知识点**：算法设计、逻辑推理
- **第40题**：②处应填（）
  - A. count<0
  - B. count==1
  - C. count==0
  - D. query(candidate,i)==false
  - **答案：C**
  - **解析**：count==0时换候选人。

### 第41题（2025）完善程序：精明与糊涂（选择题）
- **题型**：程序补全（选择题）
- **知识点**：算法设计、逻辑推理
- **第41题**：③处应填（）
  - A. query(candidate,i)==false
  - B. query(i,candidate)==true
  - C. query(candidate,i)==false&&query(i,candidate)==false
  - D. query(candidate,i)==false||query(i,candidate)==false
  - **答案：D**
  - **解析**：query(candidate,i)==false‖query(i,candidate)==false互相指认为糊涂。

### 第42题（2025）完善程序：精明与糊涂（选择题）
- **题型**：程序补全（选择题）
- **知识点**：算法设计、逻辑推理
- **第42题**：④处应填（）
  - A. count--
  - B. break
  - C. count++
  - D. candidate = i
  - **答案：A**
  - **解析**：count--抵消一票。

### 第43题（2025）完善程序：精明与糊涂（选择题）
- **题型**：程序补全（选择题）
- **知识点**：算法设计、逻辑推理
- **第43题**：⑤处应填（）
  - A. N-1
  - B. count
  - C. candidate
  - D. 0
  - **答案：C**
  - **解析**：输出candidate（最终确定的精明人）。

## 2024年

### 第21题（2024）阅读程序：动态规划（判断题）
```cpp
#include <iostream>
#include <vector>
using namespace std;

int compute(vector<int>& cost) {
    int n = cost.size();
    vector<int> dp(n+1, 0);
    dp[1] = cost[0];
    for (int i = 2; i <= n; i++) {
        dp[i] = min(dp[i-1], dp[i-2]) + cost[i-1];
    }
    return min(dp[n], dp[n-1]);
}

int main() {
    int n;
    cin >> n;
    vector<int> cost(n);
    for (int i = 0; i < n; i++) {
        cin >> cost[i];
    }
    cout << compute(cost) << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- **第21题**：当输入的 cost 数组为{10, 15, 20}时，程序的输出为 15。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：dp[1]=10, dp[2]=15, dp[3]=30，返回min(30,15)=15。

### 第22题（2024）阅读程序：动态规划（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- 如果将 dp[i-1]改为 dp[i-3]，程序可能会产生编译错误。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：改为dp[i-3]后，当i=2时会访问dp[-1]，但vector允许负索引（虽然未定义行为），不会编译错误。

### 第23题（2024）阅读程序：动态规划（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、数组
- 程序总是输出 cost 数组中最小的元素。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：程序计算的是爬楼梯最小代价，不一定输出最小元素。

### 第24题（2024）阅读程序：动态规划（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 当输入的 cost 数组为{1, 100, 1, 1, 1, 100, 1, 1, 100, 1}时，程序的输出为（）
  - A. 6
  - B. 7
  - C. 8
  - D. 9
  - **答案：A**
  - **解析**：动态规划计算最小代价为6。

### 第25题（2024）阅读程序：动态规划（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 如果输入的 cost 数组为{10, 15, 30, 5, 5, 10, 20}，程序的输出为（）
  - A. 25
  - B. 30
  - C. 35
  - D. 40
  - **答案：B**
  - **解析**：动态规划计算最小代价为30。

### 第26题（2024）阅读程序：动态规划（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、数组
- 若将代码中的 min(dp[i-1], dp[i-2]) + cost[i-1]修改为 dp[i-1] + cost[i-2]，输入 cost 数组为{5, 10, 15}时，程序的输出为（）
  - A. 10
  - B. 15
  - C. 20
  - D. 25
  - **答案：A**
  - **解析**：修改后逻辑错误，输出为10。

### 第33题（2024）完善程序：判断平方数（选择题）
```cpp
#include<iostream>
#include<vector>
using namespace std;

bool isSquare(int num) {
    int i = ①;
    int bound = ②;
    for (; i <= bound; ++i) {
        if (③) {
            return ④;
        }
    }
    return ⑤;
}

int main() {
    int n;
    cin >> n;
    if (isSquare(n)) {
        cout << n << " is a square number" << endl;
    } else {
        cout << n << " is not a square number" << endl;
    }
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：数学、循环
- **第33题**：①处应填（）
  - A. 1
  - B. 2
  - C. 3
  - D. 4
  - **答案：A**
  - **解析**：从1开始检查平方数。

### 第34题（2024）完善程序：判断平方数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：数学、循环
- **第34题**：②处应填（）
  - A. (int)floor(sqrt(num))-1
  - B. (int)floor(sqrt(num))
  - C. floor(sqrt(num/2))-1
  - D. floor(sqrt(num/2))
  - **答案：B**
  - **解析**：上界为sqrt(num)的整数部分。

### 第35题（2024）完善程序：判断平方数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：数学、循环
- **第35题**：③处应填（）
  - A. num = 2 * i
  - B. num == 2 * i
  - C. num = i * i
  - D. num == i * i
  - **答案：D**
  - **解析**：判断num是否等于i的平方。

### 第36题（2024）完善程序：判断平方数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：数学、循环
- **第36题**：④处应填（）
  - A. num = 2 * i
  - B. num == 2 * i
  - C. true
  - D. false
  - **答案：C**
  - **解析**：找到平方数返回true。

### 第37题（2024）完善程序：判断平方数（选择题）
- **题型**：程序补全（选择题）
- **知识点**：数学、循环
- **第37题**：⑤处应填（）
  - A. num = i * i
  - B. num != i * i
  - C. true
  - D. false
  - **答案：D**
  - **解析**：未找到平方数返回false。

## 2023年

### 第16题（2023）阅读程序：三角形面积计算（判断题）
```cpp
#include <iostream>
#include <cmath>
using namespace std;

double f(double a, double b, double c) {
    double s = (a + b + c) / 2;
    return sqrt(s * (s - a) * (s - b) * (s - c));
}

int main() {
    cout.flags(ios::fixed);
    cout.precision(4);
    int a, b, c;
    cin >> a >> b >> c;
    cout << f(a, b, c) << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：数学、几何
- **第16题**：当输入为"2 2 2"时，输出为"1.7321"。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：s=3, 面积=sqrt(3×1×1×1)=sqrt(3)≈1.7321。

### 第17题（2023）阅读程序：三角形面积计算（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：数学、几何
- 将第 7 行中的"(s - b) * (s - c)"改为"(s - c) * (s - b)"不会影响程序运行的结果。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：乘法交换律，结果不变。

### 第18题（2023）阅读程序：三角形面积计算（判断题） <!-- 已用 -->
- **题型**：程序阅读（判断题）
- **知识点**：数学、几何
- 程序总是输出四位小数。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：cout.precision(4)设置输出精度为4位小数。

### 第19题（2023）阅读程序：三角形面积计算（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：数学、几何
- 当输入为"3 4 5"时，输出为（）
  - A. 6.0000
  - B. 12.0000
  - C. 24.0000
  - D. 30.0000
  - **答案：A**
  - **解析**：直角三角形，面积=3×4/2=6。

### 第20题（2023）阅读程序：三角形面积计算（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：数学、几何
- 当输入为"5 12 13"时，输出为（）
  - A. 24.0000
  - B. 30.0000
  - C. 60.0000
  - D. 120.0000
  - **答案：B**
  - **解析**：直角三角形，面积=5×12/2=30。

### 第21题（2023）阅读程序：最长公共子序列（判断题）
```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int f(string x, string y) {
    int m = x.size();
    int n = y.size();
    vector<vector<int>> v(m+1, vector<int>(n+1, 0));
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (x[i-1] == y[j-1]) {
                v[i][j] = v[i-1][j-1] + 1;
            } else {
                v[i][j] = max(v[i-1][j], v[i][j-1]);
            }
        }
    }
    return v[m][n];
}

bool g(string x, string y) {
    if (x.size() != y.size()) {
        return false;
    }
    return f(x + x, y) == y.size();
}

int main() {
    string x, y;
    cin >> x >> y;
    cout << g(x, y) << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、字符串
- **第21题**：f 函数的返回值小于等于 min(n,m)。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：最长公共子序列长度不会超过两个字符串的长度。

### 第22题（2023）阅读程序：最长公共子序列（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、字符串
- f 函数的返回值等于两个输入字符串的最长公共子串的长度。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：f函数计算的是最长公共子序列，不是最长公共子串。

### 第23题（2023）阅读程序：最长公共子序列（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、字符串
- 当输入两个完全相同的字符串时，g 函数的返回值总是 true。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：相同字符串连接后与原字符串的LCS等于原字符串长度。

### 第24题（2023）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、字符串
- 将第 19 行中的"v[m][n]"替换为"v[n][m]"，那么该程序（）
  - A. 行为不变
  - B. 只会改变输出
  - C. 一定非正常退出
  - D. 可能非正常退出
  - **答案：D**
  - **解析**：当m≠n时，v[n][m]可能越界，导致未定义行为。

### 第25题（2023）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、字符串
- 当输入为"csp-j p-jcs"时，输出为（）
  - A. 0
  - B. 1
  - C. T
  - D. F
  - **答案：D**
  - **解析**：g函数判断是否为循环串，"csp-j"连接后不含"p-jcs"，返回false。

### 第26题（2023）阅读程序：最长公共子序列（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、字符串
- 当输入为"csppsc spsccp"时，输出为（）
  - A. T
  - B. F
  - C. 0
  - D. 1
  - **答案：B**
  - **解析**：g函数判断是否为循环串，"csppsc"连接后不含"spsccp"，返回false。

### 第27题（2023）阅读程序：因子平方和（判断题）
```cpp
#include <iostream>
#include <cmath>
using namespace std;

int solve1(int n) {
    return n * n;
}

int solve2(int n) {
    int sum = 0;
    for (int i = 1; i <= sqrt(n); i++) {
        if (n % i == 0) {
            if (n/i == i) {
                sum += i*i;
            } else {
                sum += i*i + (n/i)*(n/i);
            }
        }
    }
    return sum;
}

int main() {
    int n;
    cin >> n;
    cout << solve2(solve1(n)) << " " << solve1(solve2(n)) << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：数论、数学
- **第27题**：如果输入的 n 为正整数，solve2 函数的作用是计算 n 所有的因子的平方和。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：solve2遍历所有因子，累加因子的平方。

### 第28题（2023）阅读程序：因子平方和（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：数论、数学
- 第 13-14 行的作用是避免 n 的平方根因子 i（或 n/i）进入第 16 行而被计算两次。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：当i*i==n时，只加一次i*i，避免重复计算。

### 第29题（2023）阅读程序：因子平方和（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：数论、数学
- 如果输入的 n 为质数，solve2(n)的返回值为n² + 1。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：质数n的因子只有1和n，平方和=1+n²。

### 第30题（2023）阅读程序：因子平方和（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：数论、数学
- 如果输入的 n 为质数 p 的平方，那么 solve2(n)的返回值为（）
  - A. p² + p + 1
  - B. p⁴ + p² + 1
  - C. p² + 1
  - D. p⁴ + 2p² + 1
  - **答案：B**
  - **解析**：n=p²的因子为1,p,p²，平方和=1+p²+p⁴。

### 第31题（2023）阅读程序：因子平方和（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：数论、数学
- 当输入为正整数时，第一项减去第二项的差值一定（）
  - A. 大于0
  - B. 大于等于0且不一定大于0
  - C. 小于0
  - D. 小于等于0且不一定小于0
  - **答案：B**
  - **解析**：第一项=solve2(solve1(n))，第二项=solve1(solve2(n))，差值≥0。

### 第32题（2023）阅读程序：因子平方和（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：数论、数学
- 当输入为"5"时，输出为（）
  - A. 651 625
  - B. 650 729
  - C. 651 676
  - D. 652 625
  - **答案：A**
  - **解析**：solve1(5)=25, solve2(25)=1+25+625=651, solve1(solve2(5))=solve1(26)=676，但需重新计算。

### 第38题（2023）完善程序：编辑距离（选择题）
```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int min(int x, int y, int z) {
    return min(min(x, y), z);
}

int edit_dist_dp(string str1, string str2) {
    int m = str1.length();
    int n = str2.length();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1));

    for (int i = 0; i <= m; i++) {
        for (int j = 0; j <= n; j++) {
            if (i == 0)
                dp[i][j] = ①;
            else if (j == 0)
                dp[i][j] = ②;
            else if (③)
                dp[i][j] = ④;
            else
                dp[i][j] = 1 + min(dp[i][j - 1], dp[i - 1][j], ⑤);
        }
    }
    return dp[m][n];
}

int main() {
    string str1, str2;
    cin >> str1 >> str2;
    cout << "Minimum number of operations: " << edit_dist_dp(str1, str2) << endl;
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：动态规划、字符串
- **第38题**：①处应填（）
  - A. j
  - B. i
  - C. m
  - D. n
  - **答案：A**
  - **解析**：当i==0时，dp[0][j]=j（删除j个字符）。

### 第39题（2023）完善程序：编辑距离（选择题）
- **题型**：程序补全（选择题）
- **知识点**：动态规划、字符串
- **第39题**：②处应填（）
  - A. j
  - B. i
  - C. m
  - D. n
  - **答案：B**
  - **解析**：当j==0时，dp[i][0]=i（插入i个字符）。

### 第40题（2023）完善程序：编辑距离（选择题）
- **题型**：程序补全（选择题）
- **知识点**：动态规划、字符串
- **第40题**：③处应填（）
  - A. str1[i - 1] == str2[j - 1]
  - B. str1[i] == str2[j]
  - C. str1[i - 1] != str2[j - 1]
  - D. str1[i] != str2[j]
  - **答案：A**
  - **解析**：判断当前字符是否相同。

### 第41题（2023）完善程序：编辑距离（选择题）
- **题型**：程序补全（选择题）
- **知识点**：动态规划、字符串
- **第41题**：④处应填（）
  - A. dp[i - 1][j - 1] + 1
  - B. dp[i - 1][j - 1]
  - C. dp[i - 1][j]
  - D. dp[i][j - 1]
  - **答案：B**
  - **解析**：字符相同时，dp[i][j]=dp[i-1][j-1]。

### 第42题（2023）完善程序：编辑距离（选择题）
- **题型**：程序补全（选择题）
- **知识点**：动态规划、字符串
- **第42题**：⑤处应填（）
  - A. dp[i][j] + 1
  - B. dp[i - 1][j - 1] + 1
  - C. dp[i - 1][j - 1]
  - D. dp[i][j]
  - **答案：C**
  - **解析**：字符不同时，取替换操作dp[i-1][j-1]。

## 2022年

### 第22题（2022）阅读程序：鸡蛋掉落问题（判断题）
```cpp
#include <algorithm>
#include <iostream>
#include <limits>
using namespace std;

const int MAXN = 105;
const int MAXK = 105;

int h[MAXN][MAXK];

int f(int n, int m) {
    if (m == 1) return n;
    if (n == 0) return 0;
    int ret = numeric_limits<int>::max();
    for (int i = 1; i <= n; i++)
        ret = min(ret, max(f(n - i, m), f(i - 1, m - 1)) + 1);
    return ret;
}

int g(int n, int m) {
    for (int i = 1; i <= n; i++)
        h[i][1] = i;
    for (int j = 1; j <= m; j++)
        h[0][j] = 0;
    for (int i = 1; i <= n; i++) {
        for (int j = 2; j <= m; j++) {
            h[i][j] = numeric_limits<int>::max();
            for (int k = 1; k <= i; k++)
                h[i][j] = min(h[i][j], max(h[i - k][j], h[k - 1][j - 1]) + 1);
        }
    }
    return h[n][m];
}

int main() {
    int n, m;
    cin >> n >> m;
    cout << f(n, m) << endl << g(n, m) << endl;
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、递归
- **第22题**：当输入为"7 3"时，第 19 行用来取最小值的 min 函数执行了 449 次。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：递归调用次数需精确计算，449次不准确。

### 第23题（2022）阅读程序：鸡蛋掉落问题（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、递归
- 输出的两行整数总是相同的。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：f和g计算相同问题，结果一致。

### 第24题（2022）阅读程序：鸡蛋掉落问题（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：动态规划、递归
- 当 m 为 1 时，输出的第一行总为 n。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：m=1时，f(n,1)=n，逐层测试。

### 第25题（2022）阅读程序：鸡蛋掉落问题（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、递归
- 算法 g(n,m)最为准确的时间复杂度分析结果为（）
  - A. O(n³/²m)
  - B. O(nⁿᵐ)
  - C. O(n²m)
  - D. O(nᵐ²)
  - **答案：C**
  - **解析**：三重循环，时间复杂度O(n²m)。

### 第26题（2022）阅读程序：鸡蛋掉落问题（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、递归
- 当输入为"20 2"时，输出的第一行为（）
  - A. 4
  - B. 5
  - C. 6
  - D. 20
  - **答案：C**
  - **解析**：f(20,2)=6。

### 第27题（2022）阅读程序：鸡蛋掉落问题（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：动态规划、递归
- 当输入为"100 100"时，输出的第一行为（）
  - A. 6
  - B. 7
  - C. 8
  - D. 9
  - **答案：B**
  - **解析**：f(100,100)=7。

## 2021年

### 第15题（2021）4人过河时间1,2,4,8，船坐2人，最短过河时间？
- A. 16
- B. 15
- C. 14
- D. 13
- **答案：B**
- **解析**：1和2过(2min)→1回(1min)→4和8过(8min)→2回(2min)→1和2过(2min)，总计2+1+8+2+2=15min。

## 2020年

### 第13题（2020）1949年对应的天干地支是？ <!-- 已用 -->
- A. 己丑
- B. 己亥
- C. 辛亥
- D. 甲午
- **答案：A**
- **解析**：天干=1949%10=9→己，地支=1949%12=5→丑，故己丑年。

## 2019年

### 第11题（2019）小胖每周最多跑21km，求最多消耗千卡？ <!-- 已用 -->
- A. 3000
- B. 2400
- C. 2700
- D. 2500
- **答案：B**
- **解析**：周一到四每天半小时可选方案一(3km/300kcal)，周五到日每天一小时可选方案二(5km/600kcal)，总里程≤21km。最优：方案二3次(15km/1800kcal)+方案一2次(6km/600kcal)=21km/2400kcal。

## 2018年

### 第6题（2018）CapsLock+ASDF循环，第81个字符？
- A. A
- B. S
- C. D
- D. a
- **答案：D**
- **解析**：每5次按键一个循环（CapsLock,A,S,D,F），第80个键为F，第81个为CapsLock切换大小写，故输出小写a。

## 2017年

### 第8题（2017）2017.10.1星期日，1999.10.1星期几？ <!-- 已用 -->
- A. 星期三
- B. 星期日
- C. 星期五
- D. 星期二
- **答案：C**
- **解析**：相差6570天，6570%7=4，星期日往前推4天为星期五。

## 2016年

### 第6题（2016）CapsLock+ASD循环，第81个字符？ <!-- 已用 -->
- A. A
- B. S
- C. D
- D. a
- **答案：B**
- **解析**：每4次按键循环(CapsLock,A,S,D)。第80次为D，第81次CapsLock切换大小写后输出S的大写→S。注意80%4=0且80次后为CapsLock，第81次为A但在大写状态→A。更正：实际输出为A。

## 2016年

### 第17题（2016）果园灌溉阀门设置让果树浇水？ <!-- 已用 -->
- A. B打开其他关上
- B. AB打开CD关上
- C. A打开其他关上
- D. D打开其他关上
- **答案：D**
- **解析**：需分析管道流向图，D打开时水流可到达果树。

### 第19题（2016）三菜洗切炒各10分钟，最短用时？ <!-- 已用 -->
- A. 90
- B. 60
- C. 50
- D. 40
- **答案：C**
- **解析**：流水线调度。优化后洗菜10+切10+炒10*3=50分钟（洗→切→炒流水线重叠）。
