# 图论

## 2019年

### 第16题（2019）阅读程序：图匹配（判断题）
```cpp
#include <cstdio>
using namespace std;
int n, m;
int a[100], b[100];

int main() {
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; ++i)
        a[i] = b[i] = 0;
    for (int i = 1; i <= m; ++i) {
        int x, y;
        scanf("%d%d", &x, &y);
        if (a[x] < y && b[y] < x) {
            if (a[x] > 0)
                b[a[x]] = 0;
            if (b[y] > 0)
                a[b[y]] = 0;
            a[x] = y;
            b[y] = x;
        }
    }
    int ans = 0;
    for (int i = 1; i <= n; ++i) {
        if (a[i] == 0)
            ++ans;
        if (b[i] == 0)
            ++ans;
    }
    printf("%d\n", ans);
    return 0;
}
```
- **题型**：程序阅读（判断题）
- **知识点**：图论、匹配
- **第1题**：当 m>0 时，输出的值一定小于 2n。（）
  - A. 正确
  - B. 错误
  - **答案：A**
  - **解析**：每次匹配成功会减少ans，最终ans<2n。

### 第17题（2019）阅读程序：图匹配（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：图论、匹配
- 执行完第 27 行的 "++ans" 时，ans 一定是偶数。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：ans每次加1，可能为奇数。

### 第18题（2019）阅读程序：图匹配（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：图论、匹配
- a[i]和 b[i]不可能同时大于 0。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：可能同时大于0，例如x和y匹配后。

### 第19题（2019）阅读程序：图匹配（判断题）
- **题型**：程序阅读（判断题）
- **知识点**：图论、匹配
- 若程序执行到第 13 行时，x 总是小于 y，那么第 15 行不会被执行。（）
  - A. 正确
  - B. 错误
  - **答案：B**
  - **解析**：x<y时，b[y]<x可能成立，第15行可能执行。

### 第20题（2019）阅读程序：图匹配（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：图论、匹配
- 若 m 个 x 两两不同，且 m 个 y 两两不同，则输出的值为（）
  - A. 2n+2
  - B. 2n
  - C. 2n-2m
  - D. 2n-2
  - **答案：C**
  - **解析**：m对不同匹配减少2m个未匹配点。

### 第21题（2019）阅读程序：图匹配（选择题）
- **题型**：程序阅读（选择题）
- **知识点**：图论、匹配
- 若 m 个 x 两两不同，且 m 个 y 都相等，则输出的值为（）
  - A. 2n-2
  - B. 2m
  - C. 2n-2m
  - D. 2n
  - **答案：A**
  - **解析**：y相同时只有最后一个匹配有效，其余被覆盖。

## 2025年

### 第5题（2025）有向图所有顶点入度之和等于出度之和，这个总和等于？
- A. 顶点数
- B. 边数
- C. 顶点数+边数
- D. 顶点数²
- **答案：B**
- **解析**：每条有向边贡献一个出度一个入度，故入度和=出度和=边数。

## 2024年

### 第11题（2024）无向图所有顶点度数之和等于？
- A. 图的边数
- B. 图的边数的两倍
- C. 图的顶点数
- D. 图的顶点数的两倍
- **答案：B**
- **解析**：每条无向边贡献2个度（两个端点各+1），故总度数=2倍边数。

## 2023年

### 第12题（2023）有向边(1,2),(1,3),(2,4),(3,4)的拓扑排序？
- A. 1,2,3,4
- B. 1,3,2,4
- C. 2,1,3,4
- D. 1,2,4,3
- **答案：A**
- **解析**：拓扑排序要求每条边起点排在终点前。只有A选项1,2,3,4满足所有边方向。

## 2022年

### 第9题（2022）N顶点有向连通图邻接矩阵至少多少非零元素？
- A. N-1
- B. N
- C. N+1
- D. N²
- **答案：A**
- **解析**：有向连通图至少需要N-1条边（树形），每条边在邻接矩阵中对应1个非零元素。

### 第40题（2022）完善程序：洪水填充BFS（选择题）
```cpp
#include <bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c) : r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt, int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);
    int prev_color = image[cur.r][cur.c];
    ②;
    while (!queue.empty()) {
        Point pt = queue.front();
        queue.pop();
        Point points[4] = {③, Point(pt.r - 1, pt.c),
                          Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};
    Point cur(4, 4);
    char new_color = 'y';
    flood_fill(image, cur, new_color);
    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << " ";
        }
        cout << endl;
    }
    return 0;
}
```
- **题型**：程序补全（选择题）
- **知识点**：图论、BFS
- **第40题**：①处应填（）
  - A. image[r][c] == prev_color
  - B. image[r][c] != prev_color
  - C. image[r][c] == new_color
  - D. image[r][c] != new_color
  - **答案：A**
  - **解析**：判断像素颜色是否与起始像素相同。

### 第41题（2022）完善程序：洪水填充BFS（选择题）
- **题型**：程序补全（选择题）
- **知识点**：图论、BFS
- **第41题**：②处应填（）
  - A. image[cur.r+1][cur.c] = new_color
  - B. image[cur.r][cur.c] = new_color
  - C. image[cur.r][cur.c+1] = new_color
  - D. image[cur.r][cur.c] = prev_color
  - **答案：B**
  - **解析**：将起始像素颜色改为新颜色。

### 第42题（2022）完善程序：洪水填充BFS（选择题）
- **题型**：程序补全（选择题）
- **知识点**：图论、BFS
- **第42题**：③处应填（）
  - A. Point(pt.r, pt.c)
  - B. Point(pt.r, pt.c+1)
  - C. Point(pt.r+1, pt.c)
  - D. Point(pt.r+1, pt.c+1)
  - **答案：C**
  - **解析**：四个方向中的向下方向。

### 第43题（2022）完善程序：洪水填充BFS（选择题）
- **题型**：程序补全（选择题）
- **知识点**：图论、BFS
- **第43题**：④处应填（）
  - A. prev_color = image[p.r][p.c]
  - B. new_color = image[p.r][p.c]
  - C. image[p.r][p.c] = prev_color
  - D. image[p.r][p.c] = new_color
  - **答案：D**
  - **解析**：将相邻像素颜色改为新颜色。

### 第44题（2022）完善程序：洪水填充BFS（选择题）
- **题型**：程序补全（选择题）
- **知识点**：图论、BFS
- **第44题**：⑤处应填（）
  - A. queue.push(p)
  - B. queue.push(pt)
  - C. queue.push(cur)
  - D. queue.push(Point(ROWS,COLS))
  - **答案：A**
  - **解析**：将相邻像素加入队列继续BFS。

## 2021年

### 第6题（2021）n顶点m边无向连通图(m>n)，删多少边成树？
- A. m-n+1
- B. m-n
- C. m-n-1
- D. n-m+1
- **答案：A**
- **解析**：树有n-1条边。需删除m-(n-1)=m-n+1条边。

## 2021年

### 第14题（2021）以a为起点DFS遍历，b,c,d,e中最后一个遍历的点有几个？
- A. 1
- B. 2
- C. 3
- D. 4
- **答案：B**
- **解析**：DFS路径不同会导致最后遍历的点不同。b和e可能成为最后一个，c和d不可能。

## 2020年

### 第8题（2020）10个顶点的无向图至少多少条边才能确保连通？
- A. 9
- B. 10
- C. 11
- D. 45
- **答案：A**
- **解析**：n个顶点的连通图至少需要n-1=9条边（树形结构）。

## 2018年

### 第11题（2018）4个无区别节点简单无向连通图个数？
- A. 6
- B. 7
- C. 8
- D. 9
- **答案：A**
- **解析**：4个节点的无向连通非同构图为6种（树形3种+含环3种）。

## 2017年

### 第10题（2017）n结点m边连通图删多少边变树？
- A. m-n+1
- B. m-n
- C. m+n+1
- D. n-m+1
- **答案：A**
- **解析**：树有n-1条边，需删m-(n-1)=m-n+1条边。

## 2016年

### 第15题（2016）16条边每个顶点度数为2的无向图有几个顶点？
- A. 10
- B. 12
- C. 8
- D. 16
- **答案：D**
- **解析**：总度数=16×2=32，顶点数=32÷2=16。

## 2016年

### 第18题（2016）社交网络关系图，Lucia不想让Jacob看见照片？
- A. Dana,Michael,Eve
- B. Dana,Eve,Monica
- C. Michael,Eve,Jacob
- D. Micheal,Peter,Monica
- **答案：B**
- **解析**：需要找出所有不含Jacob可达路径的朋友集合，Dana/Eve/Monica符合。
