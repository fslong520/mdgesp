---
title: "6 月 20 日作业 - 初识动态规划"
description: DP概念、01背包、状态转移、记忆化搜索、递推
---

<uyk class="water">

---

# 🎒 初识动态规划

---

# 一、选择题（每题 10 分，共 100 分）

---

## 第 1 题【一维数组】(10分)
执行以下代码，输出什么？
```cpp
int a[5] = {2, 4, 6, 8, 10};
int sum = 0;
for (int i = 1; i < 4; i++)
    sum += a[i];
cout << sum;
```

- A. 20
- B. 18
- C. 12
- D. 24

---

## 第 2 题【字符串】(10分)
执行以下代码，输出什么？
```cpp
string s = "dynamic";
cout << s.find('a');
```

- A. 1
- B. 2
- C. 4
- D. 3

---

## 第 3 题【递归】(10分)
执行以下代码，输出什么？
```cpp
int f(int n) {
    if (n <= 1) return n;
    return f(n - 1) + f(n - 2);
}
cout << f(5);
```

- A. 5
- B. 6
- C. 8
- D. 10

---

## 第 4 题【排序算法】(10分)
用选择排序对数组 `[5, 3, 8, 1, 9]` 从小到大排序，第一趟结束后数组变成？

- A. `[3, 5, 8, 1, 9]`
- B. `[1, 5, 3, 8, 9]`
- C. `[1, 3, 8, 5, 9]`
- D. `[5, 1, 8, 3, 9]`

---

## 第 5 题【时间复杂度】(10分)
以下代码的时间复杂度是？
```cpp
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        cout << "*";
```

- A. O(n)
- B. O(n²)
- C. O(2ⁿ)
- D. O(n log n)

---

## 第 6 题【指针】(10分)
以下关于指针的说法，正确的是？

- A. 指针变量存储的是普通整数值
- B. 指针只能用 int* 声明，不能用于其他类型
- C. 声明指针时必须同时初始化
- D. 指针变量存储的是内存地址

---

## 第 7 题【结构体】(10分)
执行以下代码，输出什么？
```cpp
struct Game {
    string name;
    int level;
};
Game g;
g.name = "Mario";
g.level = 5;
cout << g.name;
```

- A. Mario
- B. 5
- C. name
- D. Game

---

## 第 8 题【指针运算】(10分)
执行以下代码，输出什么？
```cpp
int a[4] = {10, 20, 30, 40};
int *p = a + 2;
cout << *p;
```

- A. 10
- B. 20
- C. 30
- D. 40

---

## 第 9 题【递推】(10分)
已知递推公式：`a[1] = 1`，`a[n] = a[n-1] + 3`（n ≥ 2），则 `a[5]` 的值是？

- A. 10
- B. 13
- C. 16
- D. 7

---

## 第 10 题【二维数组】(10分)
执行以下代码，输出什么？
```cpp
int a[3][3] = {{1,2,3},{4,5,6},{7,8,9}};
cout << a[1][2];
```

- A. 6
- B. 5
- C. 8
- D. 3

---

# 二、判断题（每题 10 分，共 50 分）

---

## 第 11 题【递归】(10分)
递归函数必须要有终止条件（边界条件），否则会无限递归，最终导致栈溢出。

- T. 正确
- F. 错误

---

## 第 12 题【数组传参】(10分)
C++ 中，数组作为函数参数传递时，传递的是整个数组的副本，因此在函数内修改数组元素不会影响原数组。

- T. 正确
- F. 错误

---

## 第 13 题【排序】(10分)
冒泡排序对已经有序的数组进行排序时，时间复杂度可以达到 O(n)（通过优化提前结束）。

- T. 正确
- F. 错误

---

## 第 14 题【变量作用域】(10分)
以下代码输出 20。
```cpp
int a = 100;
int main() {
    int a = 20;
    cout << a;
    return 0;
}
```

- T. 正确
- F. 错误

---

## 第 15 题【二维数组】(10分)
在 C++ 中，二维数组在内存中是按列优先存储的（即先存完一列再存下一列）。

- T. 正确
- F. 错误

# 三、编程题（每题 100 分，共 500 分）

---

### 1、<a href="https://fslong.iok.la/problem/AWC0094A" target="_blank">生长史莱姆 (Growing Slime)</a>（100 分）

**题目描述**  
高桥君在玩一款RPG游戏。游戏中有 N 个房间排成一列，每个房间有一只怪物，强度为 V_i。  
高桥君控制一只初始强度为 W 的史莱姆，按顺序从房间 1 到 N 逐个访问。

史莱姆访问房间 i 时：
- 若怪物强度 V_i ≤ 史莱姆当前强度，则吸收怪物，强度增加 V_i
- 否则跳过，强度不变

求最终强度。

**输入格式**  
```
N W
V_1 V_2 ... V_N
```

**输出格式**  
一个整数，表示史莱姆最终强度。

**数据范围**  
1 ≤ N ≤ 2×10⁵，1 ≤ W, V_i ≤ 10⁹

**知识点**：循环模拟、64位整数

<a href="https://fslong.iok.la/problem/AWC0094A" target="_blank">提交链接</a>

---

### 2、<a href="https://fslong.iok.la/problem/AWC0094B" target="_blank">温度波动 (Temperature Fluctuations)</a>（100 分）

**题目描述**  
给定 N 天的温度记录 A_1...A_N（值 1~9），计算每个连续 K 天的窗口温度总和，求这些总和的最大值与最小值之差。

**输入格式**  
```
N K
A_1 A_2 ... A_N
```

**输出格式**  
一个整数，表示窗口总和的最大最小值之差。

**数据范围**  
1 ≤ K ≤ N ≤ 10⁵，1 ≤ A_i ≤ 9

**知识点**：前缀和、滑动窗口

<a href="https://fslong.iok.la/problem/AWC0094B" target="_blank">提交链接</a>

---

### 3、<a href="https://fslong.iok.la/problem/AWC0094C" target="_blank">抗灾通信网 (Disaster-Resistant Communication Network)</a>（100 分）

**题目描述**  
一条笔直公路沿途有 N 座城市，第 i 座城市距西端距离为 X_i。任意两城之间可铺设双向通信线路，费用为 |X_i - X_j|。

要求：任删一座城市（连同其直接相连的线路），其余所有城市仍可相互通信。求最小总费用。

**输入格式**  
```
N
X_1 X_2 ... X_N
```

**输出格式**  
一个整数，表示最小总费用。

**数据范围**  
3 ≤ N ≤ 10⁶，0 ≤ X_i ≤ 10⁹

**知识点**：排序、数学推导

<a href="https://fslong.iok.la/problem/AWC0094C" target="_blank">提交链接</a>

---

### 4、<a href="https://fslong.iok.la/problem/AWC0094D" target="_blank">选便当 (Choosing a Lunch Box)</a>（100 分）

**题目描述**  
公司需订购便当。每份便当含主菜（A 种）、配菜（B 种）、甜点（C 种），共 A×B×C 种组合。  
公司有 N 名员工，每人分配互不相同的便当，员工之间顺序有关。求分配方案数，模 10⁹+7。

**输入格式**  
```
N A B C
```

**输出格式**  
一个整数，表示方案数模 10⁹+7。

**数据范围**  
1 ≤ N ≤ 10⁶，1 ≤ A,B,C ≤ 10⁶，N ≤ A×B×C

**知识点**：排列组合、模运算

<a href="https://fslong.iok.la/problem/AWC0094D" target="_blank">提交链接</a>

---

### 5、<a href="https://fslong.iok.la/problem/P1689" target="_blank">【基础】采药</a>（100 分）

**题目描述**  
辰辰是个天资聪颖的孩子，他的梦想是成为世界上最伟大的医师。为此，他想拜附近最有威望的医师为师。医师把他带到一个到处都是草药的山洞里对他说："孩子，这个山洞里有一些不同的草药，采每一株都需要一些时间，每一株也有它自身的价值。我会给你一段时间，在这段时间里，你可以采到一些草药。如果你是一个聪明的孩子，你应该可以让采到的草药的总价值最大。"

如果你是辰辰，你能完成这个任务吗？

**输入格式**  
第一行两个整数 T 和 M（1 ≤ T ≤ 1000，1 ≤ M ≤ 100）。  
接下来 M 行，每行两个整数，分别表示采摘某株草药的时间和这株草药的价值。

**输出格式**  
一个整数，表示最大总价值。

**样例输入**
```
70 3
71 100
69 1
1 2
```
**样例输出**
```
3
```

**提示**  
这是经典的 01 背包问题。可以用 `f[i][j]` 表示前 i 株草药、用时 j 分钟的最大价值。  
转移：装得下则比较装 vs 不装，装不下则继承。

**知识点**：01背包、二维DP

<a href="https://fslong.iok.la/problem/P1689" target="_blank">提交链接</a>

---

</div>
