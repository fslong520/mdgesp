1. B (10分)
2. C (10分)
3. D (10分)
4. A (10分)
5. B (10分)
6. C (10分)
7. D (10分)
8. A (10分)
9. B (10分)
10. C (10分)
11. T (10分)
12. T (10分)
13. T (10分)
14. F (10分)
15. F (10分)
16. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    char s[105];
    cin >> s;
    cout << strlen(s) << endl;
    return 0;
}
```

17. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    char a[205], b[105];
    cin >> a >> b;
    strcat(a, b);
    cout << a << endl;
    return 0;
}
```

18. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    char s[105];
    cin >> s;
    for (int i = 0; s[i] != '\0'; i++)
        if (s[i] >= 'a' && s[i] <= 'z') s[i] -= 32;
    cout << s << endl;
    return 0;
}
```

19. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    char a[105], b[105];
    cin >> a >> b;
    int t = strcmp(a, b);
    if (t < 0) cout << -1 << endl;
    else if (t > 0) cout << 1 << endl;
    else cout << 0 << endl;
    return 0;
}
```

20. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    char a[105], b[105];
    cin >> a >> b;
    if (strcmp(a, b) == 0) cout << "SAME" << endl;
    else cout << "DIFFERENT" << endl;
    return 0;
}
```
