1. A (10分)
2. D (10分)
3. B (10分)
4. B (10分)
5. B (10分)
6. D (10分)
7. C (10分)
8. C (10分)
9. A (10分)
10. D (10分)
11. T (10分)
12. F (10分)
13. T (10分)
14. F (10分)
15. T (10分)
16. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int x, y, z;
    cin >> x >> y >> z;
    int sum = x + y + z;
    cout << sum << endl;
    cout << 100 - sum << endl;
    return 0;
}
```

17. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int m;
    cin >> m;
    if (m >= 50) m -= 10;
    cout << m << endl;
    return 0;
}
```

18. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, sum = 0;
    cin >> n;
    for (int i = 1; i <= n; i++)
        sum += i;
    cout << sum << endl;
    return 0;
}
```

19. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, cnt[7] = {0};
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        cnt[x]++;
    }
    int winner = 1;
    for (int i = 2; i <= 6; i++)
        if (cnt[i] > cnt[winner]) winner = i;
    cout << winner << endl;
    return 0;
}
```

20. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, cnt[7] = {0};
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        cnt[x]++;
    }
    for (int i = 1; i <= 6; i++)
        cout << i << ":" << cnt[i] << endl;
    return 0;
}
```
