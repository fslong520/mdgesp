1. B (10分)
2. C (10分)
3. D (10分)
4. C (10分)
5. B (10分)
6. A (10分)
7. B (10分)
8. C (10分)
9. A (10分)
10. D (10分)
11. T (10分)
12. F (10分)
13. T (10分)
14. F (10分)
15. T (10分)
16. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string s, t;
        cin >> s >> t;
        size_t pos = s.find(t);
        if (pos == string::npos) cout << -1 << endl;
        else cout << pos << endl;
    }
    return 0;
}
```

17. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string s;
        cin >> s;
        cout << s.substr(3, 4) << endl;
    }
    return 0;
}
```

18. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string s, w;
        int pos;
        cin >> s >> w >> pos;
        s.insert(pos, w);
        cout << s << endl;
    }
    return 0;
}
```

19. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string s;
        int pos, len;
        cin >> s >> pos >> len;
        s.erase(pos, len);
        cout << s << endl;
    }
    return 0;
}
```

20. cpp (100分)

参考代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string s, w;
        int pos, len;
        cin >> s >> w >> pos >> len;
        s.replace(pos, len, w);
        cout << s << endl;
    }
    return 0;
}
```
