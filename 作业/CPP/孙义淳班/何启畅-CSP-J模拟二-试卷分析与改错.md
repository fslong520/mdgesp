# CSP-J/S 模拟（二）试卷分析与改错——何启畅

**试卷**：CSP-J/S 模拟（二）（2026-08-17 命题，`CSP-J模拟题/2026/上海/CSP-J模拟题2.md`）
**作答**：2026-08-21 11:39（record 1744）
**成绩**：73/130（56.2%）｜用时 119 分 58 秒
**J 组（1–47 题，满分 100）**：73/100（**73%**）；附加题（48–59，S 组提高题）0/30（未做）

---

## 一、试卷概况

| 板块 | 题号 | 题量 | 分值 | 得分 | 得分率 |
|------|------|------|------|------|--------|
| 单选·基础知识 | 1–15 | 15 | 30 | 22 | 73.3% |
| 阅读程序·第1题 冒泡排序 | 16–23 | 8 | 12 | 7 | 58.3% |
| 阅读程序·第2题 二进制计数 | 24–30 | 7 | 10 | 7 | 70% |
| 阅读程序·第3题 公共子序列 | 31–37 | 7 | 10 | 6 | 60% |
| 完善程序·第1题 套餐限价 | 38–42 | 5 | 15 | 15 | 100% |
| 完善程序·第2题 简单路径 | 43–47 | 5 | 15 | 9 | 60% |
| **J 组合计** | 1–47 | 47 | **100** | **73** | **73%** |
| 附加题（48–59） | 48–59 | 12 | 30 | 0 | 未做 |

> 48–59 为 S 组（提高组）附加题，J 组不计分；何启畅未作答。J 组总分 130 含附加 30，未做则满分仅 100，故总成绩 73/130 实为 J 组 73/100。

## 二、错题分布

| 板块 | 错题 | 未答 |
|------|------|------|
| 单选·基础知识 | 6, 14 | 12, 15 |
| 阅读·冒泡排序 | 17, 20 | 22 |
| 阅读·二进制计数 | 29 | — |
| 阅读·公共子序列 | 33, 34, 35 | — |
| 完善·套餐限价 | — | — |
| 完善·简单路径 | — | 44, 47 |

**J 组错 8 题**（判断 4 × 1 分 + 单选 4 = 14 分）+ **J 组未答 5 题**（12、15 各 2 分，22、44、47 各 3 分 = 13 分）。失分一半来自"整题留白"——不是不会，是没敢填。

---

## 三、逐题改错（推理 + 模拟全过程）

### 单选·基础知识（6、14 错；12、15 未答）

#### 第6题（错：答 A，正 B）
> 下面代码构成的是（ ）类型的数据结构
> A. 双向链表　B. 栈　C. 循环链表　D. 队列

```cpp
struct Node { int value; Node *link; };
Node *add(Node *node, int value) {   // 头插
    Node *new_node = new Node;
    new_node->link = node;
    new_node->value = value;
    return new_node;
}
Node *del(Node *node) {              // 删头
    Node *new_node = node->link;
    delete node;
    return new_node;
}
// main: head=add(nullptr,1); head=add(head,2); head=del(head);
```

**推理**：
- `add` 把新节点插到**头部**（新节点 link 指向原头），`del` 删除**头部**。
- 只在一端（头）插入、删除 → **后进先出** = 栈。
- 排除：Node 只有一个 link 指针，**不是双向**（双向需两个指针）；不在尾部操作、无循环，不是队列/循环链表。

答 **B**。规范答法：看操作位置——"同一端进出 = 栈；一端进一端出 = 队列"。

#### 第14题（错：答 A，正 C）
> 已知一棵二叉树有 2026 个结点，则其中至多有（ ）个结点有 2 个子结点
> A. 1010　B. 1011　C. 1012　D. 1013

**推理**：设度 0/1/2 的结点数为 n0、n1、n2。
- 结点总数：n0 + n1 + n2 = 2026
- 边数：n0·0 + n1·1 + n2·2 = 边数 = 结点数 − 1 = 2025
- 二叉树性质：**n0 = n2 + 1**（叶结点 = 2 度结点 + 1）

代入：n2+1 + n1 + n2 = 2026 → **2·n2 + n1 = 2025**。要 n2 最大，n1 取最小。n1 可为 0 吗？2025 是奇数，2·n2 是偶数，n1 必须为奇数 → n1 最小取 **1**。n2 = (2025−1)/2 = **1012**。

答 **C**。选 A（1010）可能误用 `(2026−2)/2` 或数错。规范做法：列"结点数/边数/叶结点性质"三方程，n1 取最小可行值。

#### 第12题（未答，正 B）
> 一堆卡片共 2025 张，设定一个数量，不断从牌堆中取该数量的牌，记 A 表示该数量为素数的方案数，记 B 为该数量为奇数的方案数。则 A + B =（ ）
> A. 16　B. 17　C. 18　D. 19

**推理**："不断取该数量直到取完" → 该数量必须**整除 2025**（2025 = 3⁴ × 5²）。
- 方案数 = 2025 的约数个数 = (4+1)(2+1) = 15 个。
- A（素数的方案）：2025 的约数中素数 = 3、5，共 **2** 个。
- B（奇数的方案）：2025 是奇数，**所有约数都是奇数** → 15 个。
- A + B = 2 + 15 = **17** → **B**。

#### 第15题（未答，正 C）
> 从 1×2×3×…×24×25 中，最少要删掉（ ）个数才能使剩下的数的乘积是完全平方数
> A. 3　B. 4　C. 5　D. 6

**推理**：把 1..25 全部质因数分解，统计每个质数出现次数：
```
2: 12+6+3+1 = 22（偶）
3: 8+2       = 10（偶）
5: 5+1       = 6（偶）
7: 3（奇）  11: 2（偶）
13: 1（奇） 17: 1（奇） 19: 1（奇） 23: 1（奇）
```
奇数次出现的质数：7、13、17、19、23（5 个）。完全平方数要求每个质因数指数为偶数。删数时：
- 删 13、17、19、23 这 4 个数（各消一个奇次质数）。
- 7 出现 3 次，删掉数 7（或 14、21）再消一次。
- 最少删 **5** 个数（7、13、17、19、23）→ **C**。

规范做法：质因数分解 → 找奇次质数 → 一个被删数尽量多消奇次质数。

---

### 阅读·第1题 冒泡排序（17、20 错；22 未答）

```cpp
// check(n, a)：对数组 a 做一趟"冒泡扫描"，返回本趟是否发生过交换
bool check(int n, int a[])
{
    bool found = false;                  // found：本趟是否发生交换，初值 false
    for (int i = 0; i + 1 < n; ++i) {    // 从左往右扫到倒数第 2 个元素（i+1<n 保证 a[i+1] 存在）
        if (a[i] > a[i+1]) {             // 若左边元素 > 右边元素（逆序对）
            found = true;                //   标记：本趟交换过
            auto temp = a[i];            //   交换 a[i] 与 a[i+1]（大数右沉、小数左浮）
            a[i] = a[i+1];
            a[i+1] = temp;
        }
    }
    return found;                        // 本趟结束，返回"是否交换过"
}

// solve(n, a)：反复整趟扫描，直到某趟无交换（数组已有序）——即冒泡排序
void solve(int n, int a[])
{
    while (check(n, a))                  // 只要上一趟还有交换，就再来一趟
        ;                                // 循环体是空语句（分号），什么都不做
}
```

**先懂程序在干什么**：
- `check`：一趟从左到右扫描，凡 `a[i] > a[i+1]` 就交换，并记 `found = true` 表示"本趟发生过交换"。
- `solve`：反复整趟扫描，直到某一趟**一趟都没有交换**为止。
- 结论：这就是**冒泡排序**，结束时数组**升序**排列。

**破局点：逐行读懂后，哪几行是冒泡排序的命门？**

| 代码特征（看到什么） | 推理（这说明什么） |
|------|------|
| `if (a[i] > a[i+1])` 交换相邻两数 | 相邻元素比较，大数往右走 |
| `found = true` 记录"本趟交换过" | 一趟扫描有没有变动，有标记 |
| `while (check(n,a))` 反复整趟调用 | 扫完一趟**没变动就停**（found=false） |
| 三行特征合起来 | **冒泡排序**：相邻交换 + 无交换即有序 |

**一句话破局**：见到"**相邻交换 + 一趟无交换即停**"＝冒泡排序，结果必升序。
- 见"相邻交换"即排除选择/插入/归并（它们不是只换邻居）。
- 见"while 直到无交换"即确定是冒泡——因为冒泡的结束条件就是"某趟零交换"。

**逐行模拟法**（做题时在草稿纸上这样写）：
1. 抄下输入数组，标出 `found`、`i` 两个变量。
2. 每一趟 check：从左到右推 `i`，每遇逆序对就"划线交换"，同时 `found` 改 true。
3. 一趟结束看 `found`：true 则再来一趟；false 则停。
4. 数清共执行了几趟、交换了几次。

#### 第17题（错：答 T，正 F）
> 当输入参数 n ∈ [0,1024] 时，solve 可能会进入死循环（ ）

**推理**：冒泡排序每趟扫描至少会把当前最大的元素"冒"到它最终位置。最多 n−1 趟后数组必然有序，有序后某一趟 `found` 必为 false，循环终止。任何输入都有限步结束，**不可能死循环**。

**逐行模拟**（n=5，输入 `a=[5,4,3,2,1]`，把每一趟 check 内部执行写全）：
```
solve(5, [5,4,3,2,1]) 进入
┌ 第1次 while 判断 → 调 check(5, a)
│  check: found=false
│   i=0: a[0]=5 > a[1]=4 ? 是 → found=true，交换 → [4,5,3,2,1]
│   i=1: a[1]=5 > a[2]=3 ? 是 → 交换 → [4,3,5,2,1]
│   i=2: a[2]=5 > a[3]=2 ? 是 → 交换 → [4,3,2,5,1]
│   i=3: a[3]=5 > a[4]=1 ? 是 → 交换 → [4,3,2,1,5]
│  return true（本趟交换过）
├ while 体（空语句）执行
┌ 第2次 while 判断 → 调 check(5, a)
│  check: found=false
│   i=0: 4>3 ? 是 → found=true，交换 → [3,4,2,1,5]
│   i=1: 4>2 ? 是 → 交换 → [3,2,4,1,5]
│   i=2: 4>1 ? 是 → 交换 → [3,2,1,4,5]
│   i=3: 4>5 ? 否
│  return true
├ while 体执行
┌ 第3次 while 判断 → check: [2,1,3,4,5]（交换 2 次）→ return true
┌ 第4次 while 判断 → check: [1,2,3,4,5]（交换 1 次）→ return true
┌ 第5次 while 判断 → check: 一趟扫描无任何交换 → found 保持 false → return false
└ while 条件为 false，循环终止
```
结论：逆序 5 个数共跑 **4 趟**（第 5 次 check 只用于判定终止），有限步结束 → **F**。

#### 第20题（错：答 T，正 F）
> 当 solve 结束后，a 数组以降序排列（ ）

**推理**：冒泡排序逐趟把**大数往后沉**，最终小数在前、大数在后，是**升序**，不是降序。答 **F**。

#### 第22题（未答，正 B）
> 程序交换 a[i] 与 a[i+1] 的次数（ ）
> A. 小于逆序对数量　B. 等于逆序对数量　C. 大于逆序对数量　D. 与逆序对无关

**推理**：相邻交换排序中，每交换一次恰好消除 1 个逆序对（大数越过小数），所有逆序对清零时排序完成。交换总次数 **= 逆序对数量** → **B**。

> 供对照：16 题 while 空语句合法 → F；18 题升序 0 趟 → F；19 题逆序 4 趟 → F；21 题排序后 [1,1,3,4,5] → C；23 题最坏 Θ(n²) → D。何启畅全对。

---

### 阅读·第2题 二进制计数（29 错）

```cpp
// move(b, n)：把 b 当作 n 位二进制数（b[0] 是最低位），做一次"加 1"
bool move(int b[], int n)
{
    for (int i = 0; i < n; ++i) {        // 从最低位 b[0] 往高位扫
        if (b[i] == 1) { b[i] = 0; }     // 当前位是 1 → 置 0（相当于进位，继续看下一位）
        else { b[i] = 1; return true; }  // 当前位是 0 → 置 1，加 1 完成
    }
    return false;                        // 所有位都是 1 → 全部变 0，溢出归零
}

// print(n)：从全 0 开始，每次打印当前 n 位二进制，再 move 加 1，直到溢出
void print(int n)
{
    int b[n];
    for (int i = 0; i < n; ++i) b[i] = 0;   // 初始化全 0
    do {                                    // do-while：先打印一次，保证至少输出一行
        for (int i = 0; i < n; ++i) std::cout << b[i];  // 逐位输出 b[0]..b[n-1]
        std::cout << "\n";                  // 换行
    } while (move(b, n));                   // move 加 1；返回 false（溢出）时结束
}

// count(n)：数一数一共能加几次 1（即状态总数）
int count(int n)
{
    int b[100];
    for (int i = 0; i < n; ++i) b[i] = 0;   // 初始化全 0
    int c = 0;
    do { c++; } while (move(b, n));         // 每 move 成功一次 c 加 1
    return c;                               // 返回 2ⁿ
}
```

**先懂程序在干什么**：
- `move`：把 `b` 当 **n 位二进制数**，做一次"**加 1**"：从最低位 b[0] 起，逢 1 变 0（进位），遇到第一个 0 变 1 并返回 true；若全是 1，全部变 0 返回 false（溢出归零）。
- `print(n)`：依次打印 `0, 1, 2, …, 2ⁿ−1` 的 n 位二进制。
- `count(n)`：状态总数 = **2ⁿ**。

**破局点：逐行读懂后，哪几行暴露了"加 1"？**

| 代码特征（看到什么） | 推理（这说明什么） |
|------|------|
| `for (i=0; i<n; ++i)` **从下标 0 起**扫 `b[]` | 从它开始扫 = **b[0] 是最低位**（最低位才第一个参与运算） |
| `if (b[i]==1) b[i]=0;` 且**继续下一位** | 当前位是 1 → 置 0 并**进位**（1+1=10，本位留 0） |
| `else { b[i]=1; return true; }` 遇 0 就停 | 当前位是 0 → 置 1 且**进位结束**（0+1=1，无需再进位） |
| 整个循环走完返回 false | 所有位都是 1 → 111…1 + 1 = 000…0 **溢出归零** |
| `do { 打印/计数 } while (move)` | 先输出当前状态、再 +1，反复 → **枚举 0,1,2,…,2ⁿ−1 全部状态** |

**一句话破局**：**"从最低位起，逢 1 置 0 继续、遇 0 置 1 即停"＝模拟二进制加 1**；外层反复调用＝从 0 数到 2ⁿ−1。
- 认出的关键不在"二进制"三字，而在**那段循环的形态**：它和十进制竖式加法"个位满十进一"是同一结构，只是基数换成 2——`b[i]==1` 就是"满 2"，置 0 就是"进一"。
- 一旦认出是"计数器加 1"：print 输出全部状态（共 2ⁿ 行）、count 返回值 = 2ⁿ、move 总工作量均摊 Θ(2ⁿ)——三个题全部迎刃而解。

**逐行模拟**（n=2，把 move 内部每一行执行写全）：
```
print(2) 进入：b=[0,0]（初始化全 0）
┌ do 第1次：打印 b[0]b[1] = 00
│  while 判断 → 调 move(b,2):
│    i=0: b[0]==1? 否 → b[0]=1, return true     ← 最低位 0 变 1，加 1 完成
│  b 变为 [1,0]
┌ do 第2次：打印 10
│  while 判断 → move:
│    i=0: b[0]==1? 是 → b[0]=0（进位）
│    i=1: b[1]==1? 否 → b[1]=1, return true     ← 次低位 0 变 1
│  b 变为 [0,1]
┌ do 第3次：打印 01
│  while 判断 → move:
│    i=0: b[0]==1? 否 → b[0]=1, return true
│  b 变为 [1,1]
┌ do 第4次：打印 11
│  while 判断 → move:
│    i=0: 1→0;  i=1: 1→0;  全 1 → return false   ← 溢出归零
└ while 条件 false → 结束
共打印 4 = 2² 行；count(2) 同理数到 4（第28题 → D）
```

#### 第29题（错：答 C，正 B）
> print(n) 与 count(n) 的时间复杂度为（ ）
> A. Θ(2ⁿ), Θ(2ⁿ)　B. Θ(n·2ⁿ), Θ(2ⁿ)　C. Θ(n·2ⁿ), Θ(n·2ⁿ)　D. Θ(4ⁿ), Θ(2ⁿ)

**推理**：
- `print(n)`：共打印 2ⁿ 行，每行 n 个字符，输出总量 = **n·2ⁿ** → Θ(n·2ⁿ)。
- `count(n)`：循环 2ⁿ 次，每次只做 `c++`。move 虽然每趟最长扫 n 位，但**均摊**后总工作量 = 所有位翻转次数之和 = 2ⁿ + 2ⁿ⁻¹ + … + 2 ≈ 2ⁿ⁺¹，仍是 Θ(2ⁿ)。（计数器从 0 数到 2ⁿ 的总操作量是 O(2ⁿ)，这是经典均摊结论。）

**逐行模拟证明**（n=3，数 move 一共扫了多少位）：
```
状态:  000 → 001 → 010 → 011 → 100 → 101 → 110 → 111 → 000
move扫:   1      2      1     3     1     2     1     (溢出时3)
         └ b[0]每次加1都动；b[1]每2次动1次；b[2]每4次动1次
总扫描 = 8×1(最低位) + 4×1(次低位) + 2×1(最高位) + 溢出3 = 17 ≈ 2×8 = Θ(8)
```
即 n 位计数器从 0 数到 2ⁿ 的总工作量 = 2ⁿ + 2ⁿ⁻¹ + … + 1 ≈ 2ⁿ⁺¹，**Θ(2ⁿ)**，不是 n·2ⁿ。

**误区**：把"每次 move 可能 O(n)"直接乘上 2ⁿ 得 O(n·2ⁿ)——错。要算**总工作量**：所有 move 扫描长度加起来 = Σ 进位位数 = 2ⁿ⁺¹−2，仍是 Θ(2ⁿ)。

答 **B**。

> 供对照：24 题 count 有限步 → F；25 题 0 与 1 一样多 → T；26 题第5行第4列非 0 → F（第5行=数字4=000100，左数第4列=1）；27 题第523行第14列非 1 → F；28 题 count(2)=4 → D；30 题说法错误 → A。何启畅全对。

---

### 阅读·第3题 公共子序列（33、34、35 错）

```cpp
const int mod = 1'000'000'007;           // mod：取模常数，防溢出
bool filled[maxn][maxn];                 // filled[i][j]：(i,j) 是否已算过（记忆化标记）
int mem[maxn][maxn];                     // mem[i][j]：f(i,j) 的结果缓存

// solve(i, j, a, b)：返回 a 前 i 个与 b 前 j 个的公共子序列个数（含空序列）
int solve(int i, int j, int a[], int b[])
{
    if (i == 0 || j == 0) return 1;      // 边界：有一边为空，只有空序列这 1 个
    if (filled[i][j]) return mem[i][j];  // 算过就直接返回缓存（记忆化）
    filled[i][j] = true;                 // 标记本格开始计算
    int sum = (solve(i-1, j, a, b) + solve(i, j-1, a, b)) % mod;  // 容斥：先加两半，再处理重复
    if (a[i] == b[j]) {                  // 若两序列末尾元素相等
        return mem[i][j] = sum;          //   f = f(i-1,j) + f(i,j-1)（重复项与新增项抵消）
    } else {                             // 若不相等
        return mem[i][j] = (sum - solve(i-1, j-1, a, b)) % mod;  //   f 再减去 f(i-1,j-1) 排除重复
    }
}
```

**先懂程序在干什么**：设 f(i,j) = a 前 i 个与 b 前 j 个的**公共子序列个数**（空序列算 1 个）。
- 边界：i=0 或 j=0，只能取空序列 → 1。
- 容斥递推：f(i,j) = f(i−1,j) + f(i,j−1) − f(i−1,j−1)，若 a[i]==b[j] 还要加上"以 a[i] 结尾的新子序列" f(i−1,j−1)：
  - a[i] == b[j] 时：f = f(i−1,j) + f(i,j−1)（−f(i−1,j−1) 与 +f(i−1,j−1) 抵消）
  - a[i] != b[j] 时：f = f(i−1,j) + f(i,j−1) − f(i−1,j−1)（排除重复）
- `filled`/`mem`：记忆化，每对 (i,j) 只算一次。
- **功能 = 计算两个序列的公共子序列的数量**（第35题答案 B）。

**破局点：逐行读懂后，哪几行锁定"公共子序列计数"？**

| 代码特征（看到什么） | 推理（这说明什么） |
|------|------|
| 函数带两个下标 `(i, j)`、递归调 `(i-1,j)` 与 `(i,j-1)` | **二维递推**：状态是"两个序列各自的前缀"，即 DP 在序列对上做 |
| 边界 `if (i==0 \|\| j==0) return 1` | 一边为空时算 **1 个**（空序列）→ 计数题；若是"最长公共子序列"题，边界该是 0（长度） |
| `if (a[i] == b[j])` 判定两序列末尾元素 | 比较的是**元素相等**（而非大小/和差）→ 序列匹配类问题 |
| 末尾相等时 `return sum`；不相等时**减** `solve(i-1,j-1)` | 容斥：加两半再**减去重复** → 在数**个数**（计数），不是在求最长 |
| `% mod` + 记忆化 | 数可能很大、每格只算一次 → 典型**计数 DP** |

**一句话破局**：**"双下标前缀 + 空序列算 1 + 末尾相等判定 + 容斥去重"＝两个序列的公共子序列计数**。
- 与"最长公共子序列"的区分：长度题的边界是 0、递推是取 max；计数题边界是 1、递推是加减。**看边界返回值 0 还是 1、看递推是 max 还是加减**，一眼分清。
- 认出后直接出答案：功能＝计数（35=B）、复杂度＝状态数 n×m（36=B）、返回 2ⁿ 是陷阱（33=F，重复元素反例）。

**逐行模拟**（a=[1,2], b=[1,2]，按调用顺序推 f 表）：
```
solve(2,2): 先算 solve(1,2) 与 solve(2,1)
├ solve(1,2): 算 solve(0,2)=1 与 solve(1,1)
│  └ solve(1,1): a1=1==b1=1 → f(0,1)+f(1,0)=1+1=2  ← 存 mem[1][1]=2（空、[1]）
│  a1=1≠b2=2 → f(1,2)=f(0,2)+f(1,1)−f(0,1)=1+2−1=2  （空、[1]）
├ solve(2,1): a2=2≠b1=1 → f(2,1)=f(1,1)+f(2,0)−f(1,0)=2+1−1=2  （空、[2]）
a2=2==b2=2 → f(2,2)=f(1,2)+f(2,1)=2+2=4      （空、[1]、[2]、[1,2]）✓
```

#### 第33题（错：答 T，正 F）
> solve(n, n, a, a) 返回 2ⁿ（ ）

**推理**：若 a 中元素**互不相同**，a 与自身公共子序列就是 a 的全部子序列，共 2ⁿ 个。但题目没说明元素互异！若 a 有重复元素，程序按"位置组合"计数，结果会变。

**逐行模拟找反例**（a = [1,1]，b = a，n=2，按调用顺序推）：
```
solve(2,2): 先算 solve(1,2) 与 solve(2,1)
├ solve(1,2): 算 solve(0,2)=1 与 solve(1,1)
│  └ solve(1,1): a1=1==b1=1 → f(0,1)+f(1,0)=1+1=2  ← mem[1][1]=2
│  a1=1==b2=1 → f(1,2)=f(0,2)+f(1,1)=1+2=3  ← mem[1][2]=3
├ solve(2,1): a2=1==b1=1 → f(2,1)=f(1,1)+f(2,0)=2+1=3  ← mem[2][1]=3
a2=1==b2=1 → f(2,2)=f(1,2)+f(2,1)=3+3=6  ← mem[2][2]=6
```
返回 **6 ≠ 4 = 2²**。反例成立，命题不真 → **F**。
（直观解释：两个 1 位置不同，程序把"a 的 1 配 b 的 1"按位置组合计数，数出 6 个，比 2²=4 多。）

**规范答法**：判断题出现"必然 2ⁿ""一定…"字样，先问"条件够吗"——没说明元素互异就是陷阱。找反例一击毙命。

#### 第34题（错：答 F，正 T）
> 函数 solve 的返回值可能小于 0（ ）

**推理**：`(sum - solve(i-1,j-1,a,b)) % mod`——C++ 中**负数取模结果仍为负数**。`sum` 是两项取模后相加再取模，可能比 `solve(i-1,j-1) % mod` 小，相减为负、取模后仍负。所以返回值**可以小于 0**。

**模拟验证**：若 mod=10，假设 `solve(i-1,j)%mod + solve(i,j-1)%mod = 3`，而 `solve(i-1,j-1)%mod = 7`，则 `(3−7)%10 = −4 < 0`。合法。答 **T**。

> 注意：这是 C++ 负数取模的坑——`%` 结果符号与被除数一致。与数学上"模运算归到 0..mod−1"不同。

#### 第35题（错：答 D，正 B）
> 该程序的主要功能是（ ）
> A. 最长公共子序列的长度　B. 公共子序列的数量　C. 公共子串的数量　D. 相同元素个数

**推理**：
- 程序返回的是**个数**（每个 f(i,j) 是个数），不是长度 → 排除 A。
- 子序列 vs 子串：子串要求**连续**，程序递推不要求连续 → 排除 C。
- 相同元素个数是 O(n) 的简单比较，程序是二维递推 → 排除 D。
- 功能 = **计算公共子序列的数量** → **B**。

**规范答法**：功能题先排除"长度/子串/简单计数"等干扰项，再对照递推公式确认。

> 供对照：31 题 solve(2,2)=3 → T；32 题 solve(0,0)=1 → T；36 题 Θ(n·m) → B；37 题减 solve(i-1,j-1) 因排除重复 → A。何启畅全对。

---

### 完善·第1题 套餐限价（38–42 全对）

```cpp
long long S[MAXN];                       // S：B 的前缀和，S[k]=B[0]+…+B[k-1]
long long solve(int N, int M, int L, int A[], int B[])
{
    std::sort(A, A + __(1)__);           // (1) = N：A 升序排序，区间 [A, A+N)
    std::sort(B, B + M);                 // B 升序排序
    S[0] = 0;                            // 前缀和首项
    for (int i = 0; i < M; ++i) S[i+1] = S[i] + B[i];   // 算 B 前缀和
    long long j = __(2)__;               // (2) = M：分界指针从最右端开始
    long long sum = 0;                   // sum：答案
    for (int i = 0; i < N; ++i) {        // 枚举每个主食 A[i]
        while (__(3)__ && A[i] + B[j-1] > L) { j--; }   // (3) = j > 0：向左收缩，直到 A[i]+B[j-1] ≤ L
        sum += (A[i] * __(4)__);         // (4) = j：合法饮料 j 个，各加一份 A[i]
        sum += S[__(5)__];               // (5) = j：加 B[0..j-1] 之和（前缀和 S[j]）
        sum += (M - j) * L;              // 剩余 M-j 个饮料超限，各按 L 收
    }
    return sum;                          // 返回总花费
}
```

**算法骨架**（38–42 全对，此处温习）：A、B 升序排序；对每个 A[i]，B 中 `A[i]+B[k] ≤ L` 的饮料是前缀（B[0..j−1]），合法 j 个按实际价 `A[i]*j + S[j]`，非法 M−j 个按 L 收 `(M−j)*L`。答案：38=B、39=B、40=A（j>0 防越界）、41=B（j 是数量）、42=D（S[j]）。

**破局点：逐行读懂后，哪几行是命门？**（完善程序同样先逐行注释、再抓关键行，最后填空）

| 代码特征（看到什么） | 推理（这说明什么） |
|------|------|
| 两处 `sort(...)` 把 A、B 排序 | 排序后数据有序 → 多半要用**单调性**做双指针/二分 |
| `S[i+1] = S[i] + B[i]` | 前缀和 → **区间和 O(1)** 可取，不用每次累加 |
| 指针 `j` 只在 `while` 里 `j--`（只减不增） | **双指针**：随外层 i 单调移动，总步数 O(N+M)，不是 O(N·M) |
| `A[i] + B[j-1] > L` 作为收缩条件 | 用"和超限"判断分界 → 在找**合法前缀的边界** |
| `(M - j) * L` 一段按统一价收 | 分界之后整段同价 → 用前缀和 + 乘法**批量计费** |

**一句话破局**：**"排序 + 前缀和 + 单指针单调移动 + 区间批量计费"＝双指针套路**（常见于计数、求和、区间划分）。逐行读懂后填空全是送分：排序范围 (1)=N、指针初值 (2)=M、防越界 (3)=j>0、合法数量 (4)=j、前缀和取 (5)=S[j]。**双指针的均摊 O(N+M)** 是它存在的意义——凡见 j 单调移动，先想到线性。

**换你来写：如果这题让你自己写，你怎么写？**

1. **先写最笨但正确的**：双重循环枚举每对（主食 i，饮料 j），费用 `min(A[i]+B[j], L)` 全加起来——O(N·M)，小数据直接过。先保证对，再谈快。
2. **再想怎么快**：`A[i]+B[j] ≤ L` 是否成立只与 B 的大小有关——把 B 排序后，合法的必然是**一段前缀**。于是：B 排序＋前缀和 S（前缀和免每次重加）＋对每个 A[i] 找分界 j；又因 A 也升序，j 只减不增，连二分都省了——O(N+M)。
3. **对照题面代码**：正是第 2 步的实现。先暴力、后优化，这是设计程序的正常路径；亲手想过"我会怎么写"，再看题面代码，每个空都是自己思路的自然结果。


---

### 完善·第2题 简单路径（44、47 未答）

```cpp
// dfs(node)：从 node 继续枚举简单路径；每进入一个节点，cnt 增加一条"以该节点结尾"的路径
void dfs(int node)
{
    __(1)__;                    // (1) = cnt += 1：到达 node，新增一条路径
    if (cnt > limit) return;    // 超过 1024 条直接返回（不继续枚举）
    visited[node] = __(2)__;    // (2) = true：标记 node 已在当前路径中
    for (auto v : __(3)__)      // (3) = adj[node]：遍历 node 的邻居
    {
        if (not visited[v]) {   // 邻居 v 未在当前路径中，才能走
            dfs(__(4)__);       // (4) = v：递归进入邻居 v
        }
    }
    visited[node] = __(5)__;    // (5) = false：回溯时还原，允许其他路径再经过 node
}
// main 中调用：dfs(__(6)__);   // (6) = 1：从 1 号出发
```

**先懂算法骨架**：统计从 1 号出发的**简单路径条数**（点不重复）。DFS 回溯枚举：
- 每次进入 dfs(node)，代表"到达 node"形成**一条以 node 结尾的新路径** → `cnt += 1`。
- 进入 node 时标记 `visited[node] = true`（防回头）；回溯离开时恢复 `visited[node] = false`（允许其他路径再经过它）。
- 遍历邻接表 `adj[node]`，对未访问的邻居递归 `dfs(v)`。
- 入口从 1 号出发 → `dfs(1)`；超 1024 输出 −1。

**破局点：逐行读懂后，哪几行暴露"回溯枚举"？**

| 代码特征（看到什么） | 推理（这说明什么） |
|------|------|
| `for (auto v : __(3)__)` 遍历邻居 | 邻接表 `adj[node]` → **图遍历（DFS）**，非树/数组 |
| `visited[node] = __(2)__` 进点标、`__(5)__` 出点还 | 进入置 true、回溯置 false → **回溯**（普通 DFS 标记后不还原） |
| `if (not visited[v]) dfs(v)` 只走未访问点 | **简单路径**：路径中不重复走点 |
| 进函数先 `__(1)__` 计数 | 每到达一点记一条 → **统计路径条数**（进点即计数） |
| `if (cnt > limit) return` | 计数上限 → 题目要求"超 1024 输出 −1" |

**一句话破局**：**"visited 进点 true、回溯 false + 进点计数"＝DFS 回溯枚举路径条数**。
- 关键在 visited **回溯还原**：普通 DFS 找连通/可达只标不还，凡见到"回溯时还原标记"，必是**枚举所有方案**（路径、排列、子集）——这是回溯的标志性动作。
- 认出后填空即定：进点计数 (1)=cnt+=1、标记 (2)=true、还原 (5)=false、入口 (6)=1。


**换你来写：如果这题让你自己写，你怎么写？**

1. **先明确要算什么**：从 1 号出发的简单路径条数——路径上点不重复；超过 1024 条输出 −1。
2. **想算法**：图可能有环、点不能重复走，最自然的办法是带着"当前已走的点集合"去探索——走到一个点就标记它，把它的所有延伸走完后取消标记退回来，换下一条岔路继续。这正是 DFS 回溯；且每走到一个新点，就多出一条以它结尾的路径，计数 +1。（为什么不用 BFS？BFS 只适合求最短/可达，要把所有路径一条条数清，得回溯枚举。）
3. **对照题面代码**：dfs 函数就是第 2 步的直接翻译——进点计数、进 true 回 false、遍历邻居递归、入口 dfs(1)。亲手设计过一遍，这些空不需要背。

**逐空推理**：
- (1) = cnt += 1（A）：到达 node 新增一条路径，累加非倍增。
- (2)(5) = true 与 false（B）：进入标记 true，回溯还原 false。
- (3) = adj[node]（D）：遍历 node 的邻居表。
- (4) = v（C）：递归进入邻居 v。
- (6) = 1（B）：从 1 号出发。

#### 第44题（未答，正 B）
> (2)(5)处应填（ ）。A. true 与 true　B. true 与 false　C. false 与 true　D. false 与 false

**推理**：进入 node 必须标记 **true**（防止路径回头走重复点，否则图有环时无限递归）；回溯离开才还原 **false**（允许其他路径再经过它）→ **B**。

**逐行模拟**（图：1–2、2–3，对比两种填法）：
```
正确（true/false）：
  dfs(1): cnt=1, v[1]=T → 邻居 2 未访 → dfs(2)
    dfs(2): cnt=2, v[2]=T → 邻居 1 已访跳过 → 结束, v[2]=F
  v[1]=F。路径 [1]、[1,2] 共 2 条 ✓

若 (2)(5)=false/true（方向反）：
  dfs(1): cnt=1, v[1]=F → 邻居 2 "未访" → dfs(2)
    dfs(2): cnt=2, v[2]=F → 邻居 1 "未访" → dfs(1)
      dfs(1): cnt=3, v[1]=F → 邻居 2 "未访" → dfs(2) ... 无限循环 ✗
```
必须进入 true、回溯 false。

#### 第47题（未答，正 B）
> (6)处应填（ ）。A. 0　B. 1　C. N　D. cnt

**推理**：main 里只有一次 dfs 调用，统计从 **1 号**出发的路径 → `dfs(1)` → **B**。

**规范答法**：这类"入口"空是**送分点**——看 main 里唯一一次 dfs 调用 + 题干"从 1 号出发"，必填 1。下次别整段留白，先填入口空。

> 供对照：43 题 (1)=cnt+=1 → A；45 题 (3)=adj[node] → D；46 题 (4)=v → C。何启畅全对（未答的 44、47 见上）。

---

## 四、程序阅读做法（必读）

阅读程序题的核心：**先逐行看懂、再抓关键行、再模拟验证、最后按题型答题**。顺序不可颠倒——破局点是从逐行读懂里"长"出来的，不是扫一眼猜出来的；扫一眼猜错，满盘皆输。

### 第1步：逐行注释——把每行代码翻译成人话（2-3分钟，一切的基础）

拿到程序，**从第一行起逐行给代码写注释**。注释写变量**含义**（不是字面翻译），条件写**判断什么**（不是"如果成立"），动作写**为什么做**。看不懂的行先标"？"，读完一遍再回看。标准格式：

```
// check(n, a)：对数组 a 做一趟"冒泡扫描"，返回本趟是否发生过交换
bool check(int n, int a[])
{
    bool found = false;                  // found：本趟是否发生交换，初值 false
    for (int i = 0; i + 1 < n; ++i) {    // 从左往右扫到倒数第 2 个元素
        if (a[i] > a[i+1]) {             // 若左边 > 右边（逆序对）
            found = true;                //   标记：本趟交换过
            auto temp = a[i];            //   交换 a[i] 与 a[i+1]（大数右沉）
            a[i] = a[i+1];
            a[i+1] = temp;
        }
    }
    return found;                        // 返回"是否交换过"
}
```

逐行注释做完，每行干什么都清楚了，重点和关键自然浮现——下一步就是把它们挑出来。

### 第2步：找破局点——从逐行读懂的代码里抓关键行

回看刚注释完的代码，问自己："**哪几行是这段程序的命门？删掉或改动哪几行，整个程序就变了？**"把这几行关键代码挑出来，程序的功能就锁定了。

以本卷为例（各题详表见前文"破局点"小节）：

| 程序 | 逐行读完后浮现的关键行 | 锁定的功能 |
|------|------|------|
| 冒泡排序 | `a[i]>a[i+1]` 就交换＋`found` 记录本趟有无交换＋`while(check)` 到无交换为止 | 相邻交换、无交换即停＝冒泡，结果升序 |
| 二进制计数 | 从 i=0 扫起、`b[i]==1→置0` 继续、`b[i]==0→置1` 即停 | 与十进制竖式加法同构＝二进制加 1，枚举 0..2ⁿ−1 |
| 公共子序列 | 双下标递推＋边界 `return 1`（空序列算 1）＋末尾相等分支＋容斥减重 | 二维 DP 计数公共子序列 |
| 套餐限价 | sort＋指针 j 只减不增＋前缀和 S＋`(M-j)*L` 整段计费 | 双指针＋前缀和，O(N+M) |
| 简单路径 | visited 进 true 回溯 false＋进点计数＋只走未访问邻居 | 回溯枚举简单路径条数 |

**要点**：关键行不是猜的——是逐行注释后，靠"这行为什么存在"逼出来的。比如二进制计数题，只有逐行读过 `b[i]==1→置0继续 / ==0→置1即停` 的配合，才能认出它和竖式加法"满十进一"是同一结构。

### 第3步：手算模拟——拿小数据跑一遍（3-5分钟）

功能锁定后，对给定输入**逐行执行**，像电脑一样记录变量变化，验证理解。标准格式：

```
solve(5, [5,4,3,2,1]) 进入
┌ 第1次 while 判断 → 调 check(5, a)
│  check: found=false（扫描开始）
│   i=0: 5>4 ? 是 → found=true，交换 → [4,5,3,2,1]
│   i=1: 5>3 ? 是 → 交换 → [4,3,5,2,1]
│   i=2: 5>2 ? 是 → 交换 → [4,3,2,5,1]
│   i=3: 5>1 ? 是 → 交换 → [4,3,2,1,5]
│  return true（本趟交换过）
├ while 体（空语句）执行
┌ 第2次 while 判断 → 调 check(5, a) ...
```

**要点**：把每次交换/赋值后的数组或变量值**写下来**，不要心算。模拟与第2步的功能判断互相印证——对不上就回头重新读代码。

### 第4步：按题型答题（每种题型的固定套路）

**判断题**——看"必然/可能/一定/不一定"：**找反例**。只要有一个反例就答 F。
- 33 题：`solve(n,n,a,a)` 一定 2ⁿ？a=[1,1]→6≠4，反例 → F。
- 34 题：返回值可能 <0？负数取模为负，可能 → T。
- **口诀**：判断题不找依据，找反例。

**选择题**——看"一下说法正确的是/错误的是"：**算必然**，逐项排除。
- 35 题功能：先排除"长度/子串/简单计数"，剩下 B。
- 29 题复杂度：print 输出 n·2ⁿ 行 → Θ(n·2ⁿ)，count 均摊 Θ(2ⁿ)。

**输出题**——看"输出什么/第几行第几列"：**模拟结果直接读**。

**复杂度题**——看"时间复杂度/最坏情况"：**数状态数 / 算总工作量**，不是数递归层数。
- 29 题 count：总操作量 = 所有位翻转次数之和 = Θ(2ⁿ)，不是 n·2ⁿ。
- **口诀**：递归+记忆化=数格子；计数器=算总翻转数。

**功能题**——看"主要功能是"：**排除干扰项后确认**。
- 排除"长度/子串/简单计数"等干扰项。
- 对照递推公式确认是"计数"还是"长度"。

### 坑位清单（考前过一遍）

| 坑 | 例子 | 防坑口诀 |
|----|------|---------|
| 判断题"必然/一定"缺条件 | 33 题没说元素互异 | "先看条件够不够" |
| C++ 负数取模 | 34 题 % 结果可为负 | "% 符号跟被除数走" |
| 均摊复杂度 | 29 题 count 不是 n·2ⁿ | "算总工作量，不是每次乘次数" |
| 数据结构看操作位置 | 6 题同一端进出是栈 | "一端进出=栈，两端进出=队列" |
| visited 标记方向 | 44 题 false/true 反了 | "进 true 回溯 false，反了死循环" |
| 完善程序留白 | 44、47 未答 | "先填入口空和边界空，蒙也要填" |

---

## 五、程序补全做法（必读）

完善程序题的核心：**读懂算法骨架，把每个空补成让程序正确、自洽的代码**。

### 1. 先整体理解，再填空

补全前必须完成：
1. **读题干**——程序要算什么（套餐总花费、路径条数）。
2. **读完整代码**——看注释、看 main、看循环结构，先画"程序分几步"。
3. **标注变量角色**——每个数组、每个变量是干什么的：

| 题组 | 变量角色 |
|------|---------|
| 套餐限价 | A/B 排序数组、S 前缀和、j 分界指针、sum 答案 |
| 简单路径 | adj 邻接表、visited 访问标记、cnt 路径计数、limit 上限 |

4. **换位思考——如果让我写**：读罢题干，先合上题面代码自问"这个程序我会怎么写？用什么算法？"——哪怕先写出最笨的暴力，再想怎么优化；然后翻开题面代码对照。题面的每个空，本是你自己思路里的一步。


套餐题 38–42 全对，说明变量角色法已掌握；简单路径 44/47 留白是没敢填，不是不会。

### 2. 填空技巧

- **按执行顺序逐空推**：从 main 第一个空开始，跟着数据流走。
- **入口空先拿**：main 里唯一的 dfs 调用 + 题干"从 1 号出发" → (6) 必填 1（47 题送分点）。
- **标记方向**：DFS 进入 true、回溯 false（44 题），方向反了会死循环。
- **边界/终止条件先定位**：访问 `arr[k-1]` 必先保证 `k > 0`。

### 3. 代入验证（最关键）

填完全部空后，**取小样例手跑一遍**，检查每步值是否合理：
- 简单路径题：小图 1–2、1–3、2–4 手数 4 条路径，跑程序对 4 则自洽。

**不要背套路模板**——每道完善程序的骨架都要靠现场逐行读懂题面代码得来；背出来的模板换个皮就失效，读懂的能力才迁移。

### 4. 捡分策略（最重要）

**不会的题也要填**：完善程序每空 3 分，填对 1 空就有分。策略：
- 先找**入口空**（main 里唯一调用 + 题干起点）——必对。
- 再找**边界/标记空**（j>0、true/false、i==0）——按语义猜。
- 最后才想**核心逻辑空**。
- 44、47 题只要敢填（44 蒙 B 的几率 25%，47 题读完题干几乎必对），至少白捡 3–6 分。

---

## 六、小结

J 组 73/100。**失分一半来自整题留白**：12、15（单选）、22（阅读选择）、44、47（完善）共 5 题未答，白丢 13 分——这些题里有 47 这种读题干就知答案的送分题，有 44 这种二选一能蒙的题。错题本身：6 题栈与队列区分、14 题二叉树度方程、29 题均摊复杂度、33/34/35 公共子序列（反例思维 + 负数取模 + 功能识别）。**代码力不弱**：套餐限价 5 空全对，公共子序列递推模拟能对上。下步重点：
1. **不整段留白**：每空都填，先入口空、边界空，再核心空。
2. **判断题反例思维**：33 题"一定 2ⁿ"找反例；34 题想 C++ 取模符号。
3. **均摊复杂度**：计数器从 0 数到 2ⁿ 总操作 Θ(2ⁿ)。

---

*生成：2026-08-21，基于 record 1744 作答明细、`CSP-J模拟题2.md` 原题与答案表。*
